# REVIEW_2026-09-09_threat_model_redteam_v1.2 — reviewer re-review packet on docs/THREAT_MODEL.md v1.2

| Session | Date | Reviewer | Line | Status |
|---|---|---|---|---|
| Red-Team & Pen-Test Lead re-reviewing THREAT_MODEL v1.2 on the two rows this reviewer blocked in v1.1 (D-067, D-068) | 2026-09-09 | red-team-pentest-lead (3rd line) | 3rd | **Reviewer signature: SIGN WITH FINDINGS.** This packet is a review, not an approval and not a gate pass. The Product Owner decides (D-039; AI delegate under D-040). The reviewer accepts no residual risk and remediates nothing |

Reviewed at commit `a233274` (branch `claude/project-owner-agent-setup-hi3xqu` head) [Measured: `git rev-parse HEAD` → `a2332748a86c010e4d812266610ed2be587a6627`; `git status --porcelain` shows only `docs/CAPACITY_MODEL.md` and `docs/DR_PLAN.md` modified by another agent — I did not read, touch or rely on either]. Nothing here promotes any environment beyond dev/sim [Source: docs/THREAT_MODEL.md:7]. Profit is an objective, never a promise [Source: docs/THREAT_MODEL.md:7]. No regulatory position, data licence, broker capability, vendor capability or threshold is asserted anywhere in this packet.

**The standard I applied to myself.** v1.2 says the two blocking rows "were re-tested rather than re-argued, by re-running the reviewer's own probes on today's tree" [Source: git log a233274]. I did not take that on trust either. I re-ran all seven committed probes at this commit, wrote two new ones to attack the *new* claims specifically, and every number below is something I watched a process print. Where the document is right, I say so with the measurement. Where it is still wrong, the counter-example is a file anyone can run.

## 0. Roles and segregation

| Role | Seat | This session |
|---|---|---|
| Author of record, THREAT_MODEL | Security Architect (1st line) | wrote v1.2; **not** the reviewer, not the decider |
| Builder of the v1.2 controls | E09 / E11 build seats (D-068) | **not** the reviewer; TC-AUD/TC-AI quartets are the builder's own adversary model |
| Reviewer of record | Red-Team & Pen-Test Lead (3rd line) | **this packet** |
| Co-reviewer required for T-59 by TM-C12 | Independent Validation Agent | **[Open] — still has not reviewed the row** [Measured: `grep -c "T-59" docs/SESSIONS/REVIEW_2026-09-08_gate_B_iva.md` → `0`; no other IVA packet names it] |
| Decider | Product Owner (human, D-039; delegate under D-040) | pending |

I did not author, transcribe, build or approve any row under review. I edited only `docs/SESSIONS/REVIEW_2026-09-09_threat_model_redteam_v1.2.md` (this file), `docs/PENTEST/probes/rt_probe_07_witness_v12.py`, `docs/PENTEST/probes/rt_probe_08_egress_v12.py` and their captured output under `docs/PENTEST/`. **I did not edit THREAT_MODEL.md, SECURITY_PLAN.md, RAID_LOG.md, DECISION_LOG.md, AUDIT_EVIDENCE_INDEX.md or any production code**; everything owed by another seat is proposed in §7. A reviewer's signature is not an approval and I record none.

## 1. Verdict

**SIGN WITH FINDINGS.**

Plainly: the author did the hard thing. Both blocking findings were answered by changing the system, not the sentence, and the two attacks I ran on 2026-09-08 no longer work. RT-F1 is **cleared**. RT-F2 is **half cleared** — the half I actually wrote down ("nothing checks the witness unless a person asks") is gone and I could not resurrect it; the half the rows still assert ("fails closed") is still not true against the model's own declared adversary, and I have two measured counter-examples.

**Cleared, measured, not asserted.** RT-F1: `EgressPolicy` is no longer an object on a shelf — the guard is bound per tool call with that call's correlation id, denies with no policy, audits both verdicts and raises `mcp.egress_denied` [Measured: TC-AI-024..027 pass inside `251 passed`]. RT-F2 (caller half): I re-ran my own probe 01 Case A and the platform that traded to the broker on 2026-09-08 now halts — `audit.chain_verification_failed` raised **with nobody asking**, Kill Switch `['PLATFORM']`, intent `HALTED`, **0 broker submissions**; Case B no longer starts at all (`WitnessLostError: AUD-WITNESS-LOST`).

**Still blocking (the AUDIT_EVIDENCE_INDEX reviewer column should still not be filled).** Both are one-paragraph corrections by the author; neither asks for new engineering. **(RT2-F1)** The witness-floor refusal and the start-up comparison each defend against an attacker who edits *one* artefact, and the model's B10 adversary has both by definition. The ordinary rollback of Case A already deletes the seal record (1 → 0 seal rows) because the seal is written after the events it seals; add the single step the Case B attacker already had — trim the trailing lines of `audit_anchors.jsonl` — and the platform **opened, `startup_verification` ok=True, explicit `verify()` ok=True, no alerts, intent FILLED, 1 broker submission**, with five audit events erased. T-76's "a shorter chain … refused with its own reason code" is falsified by that run. **(RT2-F2)** T-77's threat is "the external witness is edited, **shortened**, removed from the middle, or lost"; the new refusal covers only "lost entirely". Shorten it instead and the platform **opened and traded** — detected as `AUD-ANCHOR-TAIL-REMOVED`, routed to `audit.anchor_missing`, auto-action `none`. Every witness-side reason code routes to the no-op alert [Source: services/audit/audit_service/store.py:56]. The row discloses the "stale while the platform is up" case and not this one.

**New, non-blocking, and the largest thing I found.** **(RT2-F7, Critical as a control finding)** the entire Case A halt rests on one line of `observability/alerts.yaml`, an unsigned plain-YAML file the composition root loads by path; `AlertRouter` defaults an uncatalogued name to `auto_action: none` and `alert.autoaction_failed` only fires when the action is *not* "none" — so **deleting the line disarms the halt silently instead of failing**. Measured: one line removed, truncated chain, platform opened, alert still fired at S2/none, Kill Switch empty, intent FILLED, 1 broker submission. **(RT2-F8, High)** `anchor_dir` is `None` by **default** in the composition root; with no publisher the witness check returns at its first line and the start-up comparison is skipped — a witnessed-then-truncated store opened, traded and verified clean with the correct witness file sitting untouched on disk. **(RT2-F5, High)** `EgressGuard.acquire()` authorises a host *string* and hands control back to the caller's own `connect` callable: a handler asked about an allowlisted host, got `EGRESS_ALLOWED` written to the audit trail, and connected somewhere else.

The wording constraint the Board set still holds and the count is still wrong: **32** rows of 96 now carry an [Open] test marker, against the "twenty" in O-143 and D-063 (a) [Measured: probe 00]. No gate report, board minute or external statement may say the threat model is covered by tests (O-143). Nothing in this programme is an independent penetration test (H-10 is not contracted).

## 2. Method and what I ran [Measured 2026-09-09, this worktree, commit `a233274`]

Read: THREAT_MODEL v1.2 header, version note, change block, reading rule, boundary line and rows T-12, T-13, T-16, T-76, T-77 (the complete change set — `git diff 178da8b a233274 -- docs/THREAT_MODEL.md` touches the header, the change block and exactly those five rows, 16 lines); my own v1.1 packet; DECISION_LOG D-066, D-067, D-068. Code read at this commit: `services/audit/audit_service/store.py` (open path, `witness_floor`, `_open_witness_check`, `_reestablish`, `verify`, `_verify_against`, `_on_failed`, reason constants), `services/audit/audit_service/reestablish.py`, `apps/web/web_bff/platform.py` (composition root: alert router, `open_store`, `AuditStore` construction, egress wiring, start-up verification), `mcp/servers/mcp_servers/egress.py`, `mcp/servers/mcp_servers/runtime.py`, `observability/rtobs/alerts.py`, `observability/alerts.yaml`, `test/quartets/test_tc_ai_egress_trust.py`.

Commands, verbatim:

```
$ RT_ENV=sim python3 -m pytest -p no:cacheprovider -o addopts="" -q
251 passed, 2 warnings in 20.44s

$ RT_ENV=sim python3 -m rt365_cli.main check --env sim
registry: OK — version 0.1.0, 6 tools, dev key, sim FIXTURE
trust anchor: none usable ... TRUST-ANCHOR-ABSENT ...
policies: OK — egress and runtime invariants hold
planes: OK — analytics cannot reach execution or the vault in-process
audit chain: OK — 20 events, hash chain intact after composition
check: OK (dev/sim evidence only; no gate is passed by this command)

$ python3 docs/PENTEST/probes/rt_probe_00_test_ids.py
threat rows: 96
rows whose test column carries [Open]: 32
absent from the test tree: ['TC-AI-016', 'TC-CP-013', 'TC-ID-007', 'TC-ID-008', 'TC-KS-011',
 'TC-MD-005', 'TC-NET-006', 'TC-PERF-004', 'TC-SC-001', 'TC-SC-002', 'TC-SEC-001',
 'TC-SEC-003', 'TC-SEC-004']
rows citing an id with no test behind it: 15 — all tagged [Open], none untagged
```

The seven probes I committed on 2026-09-08, re-run unchanged at this commit — the point of committing them:

```
$ RT_ENV=sim python3 docs/PENTEST/probes/rt_probe_01_audit_witness.py
=== Case A: audit tail truncated, platform restarted, nobody calls verify() ===
  the platform STARTED on the truncated chain: 64 events
  an intent ran after the restart: final_state=HALTED, broker submissions=0     <-- was FILLED / 1
  kill switch active levels: ['PLATFORM']                                       <-- was []
  alerts raised without calling verify(): ['audit.anchor_missing',
                                           'audit.chain_verification_failed']
=== Case B: the external witness is deleted, platform restarted ===
  WitnessLostError: AUD-WITNESS-LOST: this store recorded witnessing length 41 as anchor 3
  (seal), and the anchor store holds no record: a platform that had a witness and no longer
  has one does not open (fail closed)                                           <-- was FILLED / 1
  [the probe aborts here; Case C is no longer reachable from it — see probe 07]

$ RT_ENV=sim python3 docs/PENTEST/probes/rt_probe_03_consistent_rewrite.py
  offline: 2 journal entries for killswitch.activations removed, whole journal re-chained
  SqliteStore.verify() at open: PASSED — the rewrite is still invisible to the seam
  StoreIntegrityError: STORE-JOURNAL-ROLLBACK: the journal is at 8 but the audit chain
  witnessed 10: the store was rolled back or replaced (fail closed)             <-- was FILLED / 1

$ RT_ENV=sim python3 docs/PENTEST/probes/rt_probe_04_trust_anchor.py
  TrustAnchorRefused: TRUST-ANCHOR-ADJACENT: a trust set beside the artefact is not a trust
  anchor; the anchor for 'tool-registry' is /home/user/RT365/mcp/policies/...  <-- was ACCEPTED

$ RT_ENV=sim python3 docs/PENTEST/probes/rt_probe_05_clean_slate.py
  run 2: the same platform started against empty directories
    kill switch active levels: []
    intent: final_state=FILLED, broker submissions=1
    audit verify(): ok=True reason='' length=41 anchor_length=25
    alerts: []                                                                  <-- UNCHANGED

$ RT_ENV=sim python3 docs/PENTEST/probes/rt_probe_02_control_store.py    (unchanged behaviour)
$ RT_ENV=sim python3 docs/PENTEST/probes/rt_probe_06_consumed_grants.py  (unchanged behaviour)
```

Two new probes written for this re-review, committed and captured:

- `docs/PENTEST/probes/rt_probe_07_witness_v12.py` → `docs/PENTEST/rt_probe_07_output_2026-09-09.txt`. Cases A and B reproduce probe 01 as controls (B caught so the run continues); C attacks the witness floor; D attacks the composition-root comparison through the alert catalogue; E deletes the witness while the platform is up; F shortens the witness rather than deleting it; G starts the same composition root with `anchor_dir` omitted.
- `docs/PENTEST/probes/rt_probe_08_egress_v12.py` → `docs/PENTEST/rt_probe_08_output_2026-09-09.txt`. Case A is a handler that does not ask the guard; B is a handler that asks about one host and dials another; C measures what TC-AI-026's closure walk covers; D asks whether the socket sentinel is a property of the platform or of the test. The only socket opened is a loopback listener the probe owns; nothing leaves the host.

**These are dev/sim exercises by an internal 3rd-line role. They are not an independent penetration test and must never be recorded as one** (H-10 is not contracted; D-068 says the same).

## 3. The two blocking findings of v1.1, adjudicated

### RT-F1 — T-13 claimed an egress allowlist that no product code consulted. **CLEARED.**

What v1.1 measured: `EgressPolicy.check()` was called by no product code; the policy was loaded at `platform.py:978` and exposed as `platform.egress`, and the only caller was the test.

What I measure at this commit:

| v1.2 claim | Verdict | Evidence |
|---|---|---|
| "the allowlist is enforced at the one seam where a future handler would acquire a client, `EgressGuard`" | **true** | `EgressGuard` [Source: mcp/servers/mcp_servers/egress.py:98] is constructed in the composition root over the loaded policy [Source: apps/web/web_bff/platform.py:1132] and passed into the tool runtime [Source: apps/web/web_bff/platform.py:1140] |
| "bound per tool call with that call's correlation id, tenant, actor and tool" | **true** | [Source: mcp/servers/mcp_servers/runtime.py:250] sets a contextvar to `self._egress.bound(...)` inside the call, and copies the context into the pool thread |
| "a runtime built without a guard denies every destination" | **true** | `self._egress = egress or EgressGuard(None, ...)` [Source: mcp/servers/mcp_servers/runtime.py:101]; a guard with no policy returns `EGRESS_NO_POLICY` and raises [Source: egress.py:148-153] |
| "a denial audits the decision, raises S1 `mcp.egress_denied` and denies the call with `EGRESS_DENIED`" | **true** | [Source: egress.py:166-178]; [Source: runtime.py:263]; [Source: observability/alerts.yaml:26] |
| the tests behind it exist and pass | **true** | TC-AI-024..027 are real tests inside `251 passed` [Measured] |
| residual "the guard authorises, it does not carry traffic … evidences nothing about a deployed network" | **true and creditable** | the row discloses its own limit, which is what I asked for |

The v1.1 defect is gone. A reader of T-13 v1.2 will not conclude that egress is enforced in deployment, because the row says it is not. **RT-F1 is closed by evidence.** Three narrower defects in the same row are new findings — RT2-F3, RT2-F4, RT2-F5 in §4 — and none of them restores RT-F1.

### RT-F2 — T-12, T-16, T-76, T-77 said the platform "fails closed" against the witness when nothing checked it unless a person asked. **HALF CLEARED. The remaining half is blocking.**

**Cleared half — "only when a person asks".** This was the substance of RT-F2 and it is genuinely answered:

- The composition root now makes the published-anchor comparison at start-up, unconditionally on the caller [Source: apps/web/web_bff/platform.py:1421-1422], after the alert router and auto-actions are bound.
- A store whose own records name an absent witness refuses to open and writes nothing [Source: services/audit/audit_service/store.py:271, 592-632].
- [Measured, probe 01/07 Case A] truncated chain, restart, nobody calls `verify()` → `audit.chain_verification_failed` **and** `audit.anchor_missing` fired, `startup_verification: ok=False reason=AUD-CHAIN-HEAD-MISMATCH`, Kill Switch `['PLATFORM']`, intent `HALTED`, **broker submissions 0**. On 2026-09-08 the same file produced `FILLED` and one order at the broker.
- [Measured, probe 01/07 Case B] witness deleted, restart → `WitnessLostError: AUD-WITNESS-LOST`, the platform does not open.
- [Measured] my v1.1 RT-F3 attack — clear the alert with an ordinary operator `seal`, laundering a truncation — is refused: a seal may not create the first anchor for a non-empty chain [Source: store.py:429] and no publication may witness a chain contradicting a recorded head [Source: store.py:445, 462]; re-establishment is a named attested act [Source: services/audit/audit_service/reestablish.py] with no disable flag. Probe 01 Case C is no longer reachable because Case B now refuses first.

Every sentence quoted above from v1.2 is **literally true as written**. I could not falsify one of them.

**Not cleared — "fails closed", read against the rows' own threat statements.** The rows are true about the mechanisms and untrue about the threats they sit under. Two measured counter-examples, RT2-F1 and RT2-F2 in §4. Both are the same defect class as RT-F2 itself: a reader finishes the row believing a control refuses something it does not refuse. That is why they block, and it is why they are cheap to clear — the correction is a clause, not a control.

## 4. Findings

Severity scale (RED_TEAM_PLAN v1.1 §4): **Critical** = a control of record can be bypassed or silenced with no detection while the platform keeps trading; **High** = bypass detected but no automatic action follows, or a control not in the call path; **Medium** = the control is weaker than the row claims; **Low** = record or traceability defect.

| # | Row | What the row claims | What I measured | What would clear it | Severity | Blocking? |
|---|---|---|---|---|---|---|
| **RT2-F1** | **T-76, T-16, T-12** | T-76: "a shorter chain, a head mismatch, or a seal the witness does not carry are each refused with their own reason code"; T-16: a truncated chain "halts the platform before the next order"; T-12 (a): a store naming an absent witness "does not open at all" | The two mechanisms are **composable around**. `WitnessFloor`'s own docstring concedes that a seal record "is written after the events it seals, so a rollback of the store takes it with them" [Source: services/audit/audit_service/store.py:169-173] — and it does: [Measured, probe 07 Case C] the plain Case A rollback took the store from **1 seal record to 0**, with no extra surgery. The surviving `audit.anchor.published` mark describes only a prefix that is still intact, so it raises nothing. Add the single step the Case B attacker already performed — trim the trailing lines of `audit_anchors.jsonl` — and: **platform OPENED (58 events), `startup_verification: ok=True`, explicit `verify(): ok=True`, alerts `[]`, intent FILLED, broker submissions 1**, five audit events erased. The declared adversary of B10 is "host/OS ↔ control-store, audit-store and journal files" [Source: docs/THREAT_MODEL.md:14] — an actor with write access to *both* files by definition, which is the adversary probe 01 used and D-068 accepted | State the composition: "each mechanism defends against an attacker who edits **one** of the two artefacts; an attacker with write access to both the audit store and the anchor store — the B10 adversary — can roll them back together and is **not** detected [Measured: probe 07 Case C]. Detecting that needs a witness the platform cannot write (O-54) or a keyed head". Then T-76's "refused" sentence must be qualified rather than deleted | **High** | **Yes** |
| **RT2-F2** | **T-77** (and T-16's asymmetry paragraph) | T-77's threat: "The external witness is edited, **shortened**, removed from the middle, or lost". Claim (a): "a platform that had a witness and no longer has one does not open". Residual discloses only "a witness that is *stale or damaged while the platform is up* still pages rather than halts" | The refusal covers **"lost entirely" only**. [Measured, probe 07 Case F] chain untouched, anchor file cut from 3 records to 1 → `_open_witness_check` returns early because `latest is not None` [Source: store.py:610-611], the platform **OPENED (64 events), intent FILLED, broker submissions 1**, Kill Switch `[]`. It *was* detected — `startup_verification: ok=False reason=AUD-ANCHOR-TAIL-REMOVED` — and detection went nowhere, because **every witness-side reason code routes to the no-op alert**: `_ANCHOR_REASONS = {AUD-ANCHOR-MISSING, AUD-ANCHOR-STALE, AUD-ANCHOR-CHAIN-BROKEN, AUD-ANCHOR-TAIL-REMOVED}` [Source: store.py:56] → `audit.anchor_missing` [Source: store.py:859] → `auto_action: none` [Source: observability/alerts.yaml:15]. This is at **open**, not "while the platform is up", so the disclosed residual does not cover it. I am **not** asking for the asymmetry to be reversed — D-066 gives a reason I accept (a Kill Switch activation writes to the store under suspicion). I am asking the row to stop reading as a refusal | Add to T-77: "the refusal covers a witness that is **gone**; a witness that is **shortened, edited or broken** leaves `latest` present, so the open-time check returns and the failure is routed to `audit.anchor_missing` (auto-action `none`) — the platform opens and trades [Measured: probe 07 Case F]. Every witness-side reason code takes that path [Source: store.py:56]" | **High** as a control finding; **Medium** as the documentation defect | **Yes** |
| **RT2-F7** | **T-16, T-12** (and no row owns it) | T-16: "`audit.chain_verification_failed` carries auto-action `killswitch_platform` [Source: observability/alerts.yaml:16], so a truncated or forked chain halts the platform" | The halt rests on **one line of unsigned plain YAML**. The composition root loads it by path [Source: apps/web/web_bff/platform.py:756]; `AlertRouter.raise_alert` defaults an uncatalogued name to `{"severity": "S2", "auto_action": "none"}` [Source: observability/rtobs/alerts.py:90-94]; and the safety net `alert.autoaction_failed` fires only when `auto_action != "none"` [Source: observability/rtobs/alerts.py:102], so **deleting the line disarms the halt silently rather than failing**. [Measured, probe 07 Case D] one line removed (32 → 31), Case A's tampering unchanged → **platform OPENED, `startup_verification: ok=False reason=AUD-CHAIN-HEAD-MISMATCH`, `audit.chain_verification_failed` fired at severity=S2 auto_action=none, Kill Switch `[]`, intent FILLED, broker submissions 1**. No script, Makefile target or CI job references the file [Measured: `grep -rn "alerts.yaml" scripts/*.py Makefile .github/workflows/*.yml` → no output]. This is my own T-90 (unsigned policy files) composed with the new control, plus a **fail-open default in the alert layer** that no row covers | Two things, neither the author's alone: (a) T-16/T-12 cross-reference T-90 and say the auto-action is carried by an unsigned file; (b) a new row and a control for the alert catalogue — an uncatalogued alert name must fail loudly, not default to `none`, and the catalogue belongs with the registry's integrity story (T-89/T-90 family) | **Critical** as a control finding; dev/sim only today | No — but a **shadow entry condition** |
| **RT2-F8** | **T-12 (a) and (b), T-76** | "a store whose own records name a witness that is absent … does not open at all"; "the composition root makes the published-anchor comparison at start-up" | Both are conditional on an argument that **defaults to off**. `anchor_dir: Path | None = None` [Source: apps/web/web_bff/platform.py:732]; with no `anchor_dir` there is no publisher, so `_open_witness_check` returns at its first line [Source: store.py:598-601] and the start-up comparison is guarded by `if audit.publisher is not None` [Source: apps/web/web_bff/platform.py:1421]. [Measured, probe 07 Case G] a witnessed store, truncated as in Case A, opened with `anchor_dir` simply omitted → **platform OPENED (57 events), publisher=None, alerts `[]`, `startup_verification` never ran, explicit `verify(): ok=True`, intent FILLED, broker submissions 1** — while the correct, untampered witness file sat on disk one directory away. This is T-92 (start-up configuration as an unauthenticated control surface, accepted as residual by D-068) reaching the *new* controls, and neither row names the condition | Add the condition to T-12 and T-76: "both checks are conditional on an anchor publisher being configured; `anchor_dir` defaults to `None`, and a platform composed without one performs neither [Measured: probe 07 Case G]. Binding the platform instance to its state (T-92) is therefore load-bearing for these rows, not adjacent to them" | **High** | No — but it must be a **shadow entry condition** with T-92 |
| **RT2-F5** | **T-13** | "the allowlist is enforced at the one seam where a future handler would acquire a client, `EgressGuard`" | The seam authorises a **string** and then hands control back to the caller. `acquire(host, connect=...)` calls `check(host)` and then `return connect(host, port)` [Source: mcp/servers/mcp_servers/egress.py:179-182] — the guard never sees the destination actually dialled, and `connect` is the handler's own callable. [Measured, probe 08 Case B] a handler asked about `market-data.analytics.svc.cluster.local`, was allowed, and opened its socket to a loopback listener standing in for a non-allowlisted destination; 31 bytes arrived; the audit row the platform keeps says `host='market-data.analytics.svc.cluster.local', reason=EGRESS_ALLOWED`. The evidence names a destination that was never dialled | Say what the seam is: an **advisory authorisation** the caller may ignore or lie to, which "constrains a future handler" only if that handler is honest. A control is either the thing that opens the connection or it is a code convention. Target: the guard returns the client it built, or the enforcement is at the network, not in-process | **High** (a control not in the enforcing position, and its audit evidence is falsifiable by the party it audits) | No |
| **RT2-F4** | **T-13** | "no MCP tool handler performs network I/O and **none can**, because neither `mcp_servers` nor the first-party import closure of all six handlers contains a network module" | The first clause is true and I reproduced it. The second — "none can" — is a **capability** claim and is false. [Measured, probe 08 Case A] a replacement handler for `get_strategy_docs` (a tool that **is** in the signed registry, so `register_handler` accepted it [Source: runtime.py:110-113]) called `socket.create_connection` and **45 bytes of account state reached a destination the allowlist does not name**: `b'STOLEN account=acct-sim-001 instrument=SIMEQ1'`. `mcp.egress.denied` rows 0, `mcp.egress.allowed` rows 0, `mcp.egress_denied` alerts 0. The same handler then sent the **canary token** the same way — `b'canary=CANARY-RT365-7f3a9c'` arrived and `mcp.canary_in_output` / `mcp.canary_in_input` alerts were **0**, because the canary and payload-limit checks scan the handler's **return value** [Source: runtime.py:271-276], never its side effects. The tool call did report `ok=False`, but on `OUTPUT_SCHEMA` — a verdict reached *after* the bytes had already left | Replace "none can" with what is true: "no handler does today, and a test re-checks that on every CI run — a source-code property of the six handlers, not a capability boundary. A handler that did not ask the guard would not be stopped, audited or alerted, and the canary and payload-limit checks read the return value, not side effects [Measured: probe 08 Case A]". Note that this control is a CI check that nothing requires to pass while branch protection is off (T-94) | **Medium** (the residual already tells the reader the deployed half is unbuilt, so the row read whole does not leave egress looking enforced) | No |
| **RT2-F3** | **T-13** | "…and every tool runs under a socket sentinel [Source: test/quartets/test_tc_ai_egress_trust.py:233]" | A **test fixture cited in a control column as a platform property**, against the document's own reading rule that "a control column that says interim describes what runs today" [Source: docs/THREAT_MODEL.md:11]. [Measured, probe 08 Case D] runtime socket interception (`sys.addaudithook`, socket patching) in first-party product code: **0 occurrences** across all twenty source roots; the only file installing the sentinel is `test_tc_ai_egress_trust.py`. The sentinel exists inside TC-AI-026 for the duration of TC-AI-026 | Move the clause out of the control column into the test column, where it is excellent evidence: "TC-AI-026 runs all six tools under a socket sentinel and none opens one". Nothing runs under a sentinel in dev/sim | **Medium** | No |
| **RT2-F6** | **T-13** | the closure walk as the basis of the capability claim | Honest for today's tree and I reproduced it: 33 first-party modules walked, **0** network modules inside, **0** relative imports skipped, **0** `__import__`/`import_module` call sites [Measured, probe 08 Case C]. Two limits worth one clause: the walk does not descend into the three third-party packages the closure imports (`jsonschema`, `pydantic`, `yaml`), and **15 of the 17 banned network modules are one `__import__` away at run time** in the same process, with `socket` already resident in `sys.modules` | One clause: "the closure is first-party only; third-party packages are not walked, and Python enforces no import boundary at run time" | **Low** | No |
| **RT2-F9** | coverage statement; O-143, D-063 (a) | v1.1's list and "twenty rows" | Still wrong, and v1.2's change block says so honestly and leaves it owed [Source: docs/THREAT_MODEL.md:9]. Measured at this commit: **96 rows, 32 carrying an [Open] test marker, 15 citing an id with no test** [Measured: probe 00] | Correct the list and the number; ledger owner corrects O-143 and D-063 (a). The wording constraint stands whatever the count | **Low** | No |
| **RT2-F10** | TM-C12 | the Board required the Independent Validation Agent and me to review T-59 together | Still half satisfied. [Measured: `grep -c "T-59" docs/SESSIONS/REVIEW_2026-09-08_gate_B_iva.md` → `0`] The IVA has not reviewed the row. D-067 commissioned it; it has not happened | Commission the IVA on T-59 before the AEI row is filled. Not the author's to fix | **Low** (process) | No — but **TM-C12 remains not fully met** |
| **RT2-F11** | rows that now stand up | — | **Attacked by me at this commit and held**: probe 01 A and B (§3); the operator-seal laundering path (RT-F3, now refused by two independent guards); **probe 03** — my v1.1 RT-F4 headline, removing an engaged Kill Switch by a consistent rewrite of rows *and* journal, now fails closed with `STORE-JOURNAL-ROLLBACK: the journal is at 8 but the audit chain witnessed 10 (fail closed)` (O-128/D-066 delivered); **probe 04** — my v1.1 RT-F6 trust-anchor forgery, which took under a minute on 2026-09-08, now raises `TrustAnchorRefused: TRUST-ANCHOR-ADJACENT` at the attack step. **Still open and unchanged, as D-068 accepted**: **probe 05**, the clean-slate start (empty state directories → Kill Switch gone, intent FILLED, 1 broker submission, `verify(): ok=True`, alerts `[]`) — T-92; **probe 02**, an out-of-band delete invisible until the next open. Recorded so the signature is not read as a blanket doubt | — | — | No |

## 5. Threat-model delta this review produces

| Delta | Rows | Boundary | Evidence state |
|---|---|---|---|
| RT-F1 closed | T-13 | B6, B2 | TC-AI-024..027 inside `251 passed`; code read at four cited lines |
| RT-F2 closed on the caller half | T-12, T-16, T-76, T-77 | B4, B10 | probe 01 A and B, re-run — both v1.1 attacks now fail |
| RT-F2 open on the "fails closed" half | T-76, T-16, T-12; T-77 | B4, B10 | probe 07 C and F; **blocking** |
| The halt's auto-action rests on an unsigned file, and the alert layer fails open on an uncatalogued name | T-16, T-12; new row owed | B10, B8 | probe 07 D; `rtobs/alerts.py:90-102` |
| Both new checks are off when `anchor_dir` is omitted, which is the default | T-12, T-76; joins T-92 | B10 | probe 07 G |
| The egress seam is advisory and its audit evidence is falsifiable | T-13 | B6 | probe 08 A and B |
| A test fixture cited as a running control | T-13 | B6 | probe 08 D |
| RT-F4 (consistent control-store rewrite) and RT-F6 (trust anchor) now fail closed | T-61, T-89 | B10, B4 | probes 03 and 04 |
| T-92 (clean slate) unchanged and still open | T-92 | B10, B8 | probe 05 |

## 6. Control quartets for the two controls that changed this week

Written by the reviewer as the shape a retest would take; **the author of a quartet is not its approver, and the builder allocates the ids.** These extend, and do not replace, the quartets in my v1.1 packet §8.

**Control A — witness continuity, tested against an attacker who holds both artefacts (the gap behind RT2-F1, RT2-F2, RT2-F8).**
- *Positive*: a platform composed **with** an anchor publisher opens on an intact chain whose witness matches; the start-up comparison runs and one audit row records that it passed.
- *Negative*: the chain is short, forked or head-mismatched with the witness intact → halt before the next order (already true: probe 07 Case A). The **witness** is short, forked or broken with the chain intact → the row must state the catalogued response, and the test must assert it rather than assert an alert was merely raised (today: probe 07 Case F trades).
- *Abuse*: (i) both artefacts rolled back **together** to a consistent pair → the case that must be either refused or **explicitly disclosed as undetected** (today: probe 07 Case C trades and verifies clean); (ii) the platform composed with `anchor_dir` omitted against a store that had a witness → refused, or the absence of a publisher is itself a catalogued S1 (today: probe 07 Case G trades silently); (iii) the alert catalogue edited to remove the auto-action → the platform refuses to compose on an uncatalogued integrity alert (today: probe 07 Case D trades).
- *Recovery*: the attested re-establishment of `reestablish.py` — already built and already refuses a seal below the floor — exercised end to end, with the incident and its recovery sharing one correlation id.

**Control B — egress, if it is to be a control rather than a convention (the gap behind RT2-F4, RT2-F5).**
- *Positive*: a handler acquires an allowlisted destination through the guard, and the **object it receives is the one the guard built**, so the authorised host and the dialled host are the same fact.
- *Negative*: a non-allowlisted destination, and a guard with no policy → refused, audited, alerted (already true: TC-AI-025, TC-AI-026 (c)).
- *Abuse*: a handler that **does not ask** opens a socket → the attempt is refused or at minimum detected and alerted (today: 45 bytes left with zero audit rows and zero alerts). A handler that asks about an allowlisted host and dials another → refused or detected (today: allowed, and the audit row names the wrong host). A canary token leaving over a side channel → detected (today: not detected).
- *Recovery*: the identity whose handler attempted it is revoked, the attempt is one audit row with the call's correlation id, and the policy is unchanged by the incident (TC-AI-027 already proves the last part).

## 7. Rows proposed for artefacts I may not edit

I did not edit any of these. Ids are for their owners to allocate.

**docs/THREAT_MODEL.md** (owner: Security Architect) — the corrections in §4: **blocking RT2-F1 (T-76, T-16, T-12) and RT2-F2 (T-77)**; non-blocking RT2-F3, RT2-F4, RT2-F5, RT2-F6 (T-13), RT2-F7 and RT2-F8 (cross-references and conditions on T-12/T-16/T-76), RT2-F9 (the count). Plus **one new row owed to no one yet**: the alert catalogue as a control surface — `observability/alerts.yaml` is unsigned, is the sole carrier of every auto-action, and `AlertRouter` defaults an uncatalogued name to `auto_action: none` [Source: observability/rtobs/alerts.py:90-94], so removing a line disarms a halt silently. It belongs with T-90 and is a distinct failure mode from it. I do not write these rows and I do not approve them; a v1.2a carrying RT2-F1 and RT2-F2 is re-reviewed by me on those two rows only.

**docs/RAID_LOG.md** (owner: Program Orchestrator / Product Owner delegate; ids by the owner — I did not edit the ledger):

| Type | Proposed text | Owner | Gate |
|---|---|---|---|
| Gap | The witness-floor refusal and the start-up anchor comparison each defend against an attacker who edits one artefact; an attacker with write access to both the audit store and the anchor store rolls them back together and is not detected — the platform opens, trades and `verify()` returns ok (probe 07 Case C). Detecting it needs a witness the platform cannot write (O-54) or a keyed head | Security Architect / SRE Lead | C |
| Gap | A witness that is **shortened, edited or broken** (rather than gone) leaves `latest` present, so `_open_witness_check` returns; the failure is routed to `audit.anchor_missing`, auto-action `none`, and the platform opens and trades (probe 07 Case F). Every witness-side reason code takes that path (`_ANCHOR_REASONS`, store.py:56). O-137's confirmation of the asymmetry now covers a case discovered **at open**, not only "while up" | SRE Lead / Chief Risk Agent | C |
| Risk | `observability/alerts.yaml` is unsigned, is loaded by path by the composition root, and is the sole carrier of every auto-action; `AlertRouter` defaults an uncatalogued alert name to `auto_action: none` and `alert.autoaction_failed` fires only when the action is not "none", so **deleting one line disarms the platform halt silently** (probe 07 Case D). No script, Makefile target or CI job checks the file. Related to O-81/T-90 (unsigned policy files) but distinct: this is a fail-open default in the alert layer | SRE Lead / Security Architect | C |
| Gap | `anchor_dir` defaults to `None` in the composition root, and with no publisher neither the witness-continuity refusal nor the start-up anchor comparison runs; a witnessed-then-truncated store opened, traded and verified clean with `anchor_dir` omitted (probe 07 Case G). Both new audit controls are opt-in by argument. Folds into T-92 | Backend Lead / SRE Lead | C |
| Gap | `EgressGuard.acquire()` authorises a host string and returns the caller's own `connect` result; the guard never sees the destination dialled. A handler asked about an allowlisted host, was allowed and audited as allowed, and connected elsewhere (probe 08 Case B). The audit evidence for egress is falsifiable by the party it audits | Backend Lead / MCP Security Agent | C |
| Gap | An MCP tool handler that does not consult the guard reaches the network unimpeded, with no audit row and no alert; the canary and payload-limit checks scan the handler's return value, not its side effects, so a canary token exfiltrated over a socket raises nothing (probe 08 Case A). The control of record is a CI import ban, which nothing requires to pass while branch protection is off (O-20, T-94) | MCP Security Agent / Backend Lead | C |
| Note | THREAT_MODEL T-13 cites `test/quartets/test_tc_ai_egress_trust.py:233` (a socket sentinel installed by a test) in its **control** column; there is no runtime socket interception in first-party product code (probe 08 Case D). Evidence belongs in the test column | Security Architect | B |
| Note | O-143 and D-063 (a) say "twenty" threat-model rows name a test that does not exist. Measured at commit `a233274`: **96 rows, 32 carrying an [Open] test marker, 15 citing an id with no test**. The wording constraint is unaffected | Program Orchestrator | B |
| Note | TM-C12 requires the Independent Validation Agent and the Red-Team Lead to review T-59 together. The IVA has not (0 mentions of T-59 in any IVA packet). D-067 commissioned it | Product Owner delegate | B |
| Closed (evidence) | RT-F4's headline attack — removing an engaged Kill Switch by a consistent rewrite of rows and journal — now fails closed with `STORE-JOURNAL-ROLLBACK` (probe 03 re-run 2026-09-09). RT-F6's trust-anchor forgery now raises `TRUST-ANCHOR-ADJACENT` at the attack step (probe 04 re-run). Recorded by the reviewer who raised them; closure is the owner's to record | Program Orchestrator | B |

**docs/REQUIREMENTS_TRACEABILITY.md** (owner: Program Orchestrator) — proposed rows, extending the three in my v1.1 packet:

| Requirement | Architecture | Owner | Control | Test | Evidence | Gate |
|---|---|---|---|---|---|---|
| NFR-AUD-01 (immutable audit) | witness continuity against an attacker holding both artefacts | Security Architect / SRE Lead | a rolled-back pair is refused, or the residual is disclosed in the row | [Open — quartet A abuse (i)] | this packet; probe 07 Case C | C |
| NFR-AUD-01 | the anchor publisher as a required, not optional, composition input | Backend Lead | a platform composed without a publisher against a witnessed store is refused or raises a catalogued S1 | [Open — quartet A abuse (ii)] | this packet; probe 07 Case G | C |
| NFR-SEC-01 (integrity of control state) | the alert catalogue as an integrity-bearing artefact | SRE Lead / Security Architect | an uncatalogued integrity alert fails loudly; the catalogue's integrity is checked | [Open — quartet A abuse (iii)] | this packet; probe 07 Case D | C |
| NFR-SEC-01 (egress) | the guard in the enforcing position, or enforcement at the network | Backend Lead / MCP Security Agent | a handler that does not ask, or that dials elsewhere, is refused or detected | [Open — quartet B abuse] | this packet; probe 08 Cases A and B | C |

**docs/AUDIT_EVIDENCE_INDEX.md** (owner: Program Orchestrator; the reviewer column is filled by the reviewing role) — proposed row 2 text: `Threat model — v1.2 at commit a233274; reviewer: Red-Team & Pen-Test Lead (agent, 3rd line) — REVIEW_2026-09-09_threat_model_redteam_v1.2.md; verdict SIGN WITH FINDINGS; RT-F1 cleared by measurement, RT-F2 cleared on the caller half only; two findings still blocking (RT2-F1 witness-floor composition, RT2-F2 shortened witness); TM-C12 half met (IVA review of T-59 outstanding); Product Owner decision pending`. **The row should still not be filled until RT2-F1 and RT2-F2 are corrected and the IVA has reviewed T-59.**

**docs/DECISION_LOG.md** — nothing proposed. This packet records no decision, no approval, no gate pass and no risk acceptance.

## 8. Concerns for the Product Owner

- **RT2-PO-1 The author fixed what I broke, and I checked by breaking it again.** [Measured: probes 01, 03, 04] Three of the four attacks I demonstrated on 2026-09-08 no longer work, and I know that because I ran the same committed files, not because a document told me. Truncating the audit trail and restarting now halts the platform with zero orders at the broker; the consistent store rewrite that removed an engaged Kill Switch now fails closed; the forged trust set that widened a tool's quota by four orders of magnitude is now refused at the attack step. That is a real week's work and it is worth saying before the findings. **Decide:** nothing here — this is the record.
- **RT2-PO-2 Two rows still say "refused" about things that are not refused, and both are a clause, not a control.** [Measured: probe 07 Cases C and F] The witness-floor refusal and the start-up comparison each stop an attacker who edits one file. Edit both — which is exactly what the model's own B10 boundary describes — and the platform opens, trades and reports healthy with five audit events erased. And a witness that is *shortened* rather than deleted slips past the refusal entirely and lands on an alert whose catalogued action is `none`. **Decide:** ask for a v1.2a carrying those two disclosures and hold the AUDIT_EVIDENCE_INDEX row until it exists. I am not asking for more engineering and I am not asking to reverse the auto-action asymmetry — D-066 gives a reason for it that I accept.
- **RT2-PO-3 The platform halt you were shown depends on one line of a file nobody protects.** [Measured: probe 07 Case D] `observability/alerts.yaml` is plain unsigned YAML loaded by path, and it is the only place that says a failed audit verification should engage the Kill Switch. I deleted that one line and re-ran the attack: the platform opened on a truncated chain, the alert still fired — at S2 with no action, because an uncatalogued name silently defaults to "none" — and an order reached the broker. The safety net for a broken auto-action does not fire either, because it only checks actions that are not "none". Nothing in `scripts/`, the Makefile or CI reads that file. **Decide:** treat the alert catalogue as an integrity-bearing artefact alongside the signed registry (it is the T-90 family), and require that an uncatalogued integrity alert fails loudly rather than quietly.
- **RT2-PO-4 Both new audit controls are switched off by the default.** [Measured: probe 07 Case G] `anchor_dir` defaults to `None`. Omit it and there is no publisher, so the refusal-to-open returns at its first line and the start-up comparison is skipped. I started a truncated store that way and it traded, verified clean and raised nothing — with the correct witness file sitting untouched one directory away. This is the same shape as the clean-slate start you already accepted as residual (T-92, D-068), and it now reaches the controls that answer my blocking finding. **Decide:** make "the platform instance is bound to its state, and composing without a witness is a loud act" a shadow entry condition, and note that T-12 and T-76 depend on it.
- **RT2-PO-5 The egress control is now real, and it is advisory.** [Measured: probe 08 Cases A and B] The allowlist is genuinely on the call path — that was my finding and it is fixed. But the guard authorises and never connects: a handler that simply does not ask opened a socket and 45 bytes of account state left the process with no audit row and no alert, and the canary token — the other half of the same row's delivered control — went out the same way without raising anything, because the canary scan reads the return value, not side effects. A handler that politely asked about an allowlisted host and then dialled somewhere else was allowed, and the audit trail now records a destination that was never contacted. **Decide:** ask whether "the absence of the capability" is to remain the control. If it is, say so plainly — it is a code-review property re-checked by a CI test that nothing currently requires to pass — and stop citing a socket sentinel that exists only inside that test.
- **RT2-PO-6 One condition of the Board is still not met, and it is still not the author's fault.** [Measured: 0 mentions of T-59 in any IVA packet] TM-C12 asks the Independent Validation Agent and me to review T-59 together. I did, in v1.1. D-067 commissioned the IVA. It has not happened, and the condition is closing by attrition rather than properly. **Decide:** commission it in the same breath as the v1.2a.
- **RT2-PO-7 What none of this says.** [Source: docs/THREAT_MODEL.md:7] Nothing in this review authorises promotion beyond dev/sim, asserts that any control is fit for real money, or asserts a regulatory position, a data licence, a broker capability or a vendor capability. These are internal 3rd-line exercises in a simulated environment; H-10 (an external penetration test and red team) is not contracted, and **no gate record may describe anything in this programme as an independent penetration test** — D-068 says the same and I repeat it. Profit remains an objective, never a promise.

## 9. Assumptions, confidence, provenance

- A-1 [Measured]: every `[Measured:]` statement is a command run on 2026-09-09 in this worktree at commit `a233274`. The probe outputs quoted in §2 and §4 are verbatim and are captured at `docs/PENTEST/rt_probe_07_output_2026-09-09.txt` and `docs/PENTEST/rt_probe_08_output_2026-09-09.txt`.
- A-2 [Measured]: the full suite passes (251 tests, up from 216) and `rt365 check --env sim` returns OK at this commit. My findings are not that any test fails — they are that some rows claim more than those tests exercise, which is the same objection I raised in v1.1 and the reason I re-ran the attacks instead of reading the ids.
- A-3 [Committee]: I judge the rows against the document's own reading rule — "a control column that says interim describes what runs today in dev/sim" [Source: docs/THREAT_MODEL.md:11]. RT2-F1, RT2-F2, RT2-F3 and RT2-F4 are failures against that standard, not against a stricter one I invented.
- A-4 [Measured, scope]: probe 08 Cases A and B replace a handler for a tool that is in the signed registry, which requires in-process code execution (B8/B10). I say so plainly because the adversary matters: the finding is **not** that an untrusted prompt can exfiltrate — it cannot reach handler code. The finding is that T-13's word "can" is a capability claim, and the capability is present.
- A-5 [Measured, scope]: probe 07 Cases C, D, F and G assume write access to the host's state, anchor and policy files — boundary B10 as the document itself declares it [Source: docs/THREAT_MODEL.md:14], and the same adversary D-068 accepted for T-92 and T-94.
- A-6 [Open]: I could not verify anything about a deployed topology, an IdP, a WORM medium, a real anchor principal, a model provider, a data licence or a broker — none exists. RT2-F1's remedy in particular (a witness the platform cannot write) is untestable here and is O-54.
- A-7 [Open]: I reviewed only the five rows v1.2 changed, plus the code they cite. I did not re-review the other 91 rows, SECURITY_PLAN v1.2 (the MCP Security Agent is its reviewer), or DATA_FLOWS v1.2. My v1.1 non-blocking findings RT-F5, RT-F7..RT-F15 are unactioned by this version, which the change block states, and remain owed.
- A-8 [Open]: I did not read or rely on `docs/CAPACITY_MODEL.md` or `docs/DR_PLAN.md`; another agent is editing both in this worktree.
- A-9 [Measured, provenance]: `HEAD` moved during this session — another process made snapshot commits (`60b61ea`, `0914351`) that picked up my two probe files and their captured output while I was still running them. I did not commit, branch, stash or check out anything. **The subject of this review is unaffected**: [Measured: `git diff --stat a233274 HEAD -- docs/THREAT_MODEL.md` → empty] the document is byte-identical to the v1.2 I reviewed, and [Measured: `git diff --stat HEAD -- docs/PENTEST/` → empty] the committed probes and outputs are the final versions quoted here. Only this packet is uncommitted.
- Confidence: **high** that RT-F1 is cleared and that RT-F2's caller half is cleared — both rest on re-running the exact files that produced the v1.1 findings, and the outputs inverted. **High** in RT2-F1, RT2-F2, RT2-F7, RT2-F8, RT2-F5, RT2-F4, RT2-F3 and RT2-F6 — each is one committed file away from reproduction. **Medium** that §4 is the complete set of defects in the five changed rows; I attacked the three mechanisms the change block names and the composition root that wires them, and I did not re-model the reestablishment path in depth beyond confirming it refuses a seal below the floor. **Low** on anything concerning deployment, because there is nothing to test.
- Provenance: THREAT_MODEL v1.2 at `a233274`; my own v1.1 packet `docs/SESSIONS/REVIEW_2026-09-08_threat_model_redteam.md`; DECISION_LOG D-066, D-067, D-068; the nine probes at `docs/PENTEST/probes/` (seven committed 2026-09-08, two added by this packet) and their captured output. **This packet records no approval, no decision, no gate pass and no risk acceptance, and a reviewer's signature is not an approval.**
