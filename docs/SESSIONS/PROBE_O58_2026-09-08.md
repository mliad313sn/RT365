# PROBE O-58 — is the agent write-scope guard enforced by the harness? (2026-09-08)

| Session | Date | Environment | Status | Documented by |
|---|---|---|---|---|
| PROBE-O58 | 2026-09-08 | agent harness (Claude Code sub-agents in git worktrees) | evidence for RAID O-58 (closed) and R-46/R-51 | product-owner delegate, from three probe agents' verbatim reports |

## Probe A — frontmatter `hooks:` (agent counsel-ai-safety, worktree at d1ccb21)
- Out-of-scope `Write services/risk/probe_o58.txt` → **succeeded**: `File created successfully at: .../services/risk/probe_o58.txt`; no hook output.
- In-scope `Write docs/SESSIONS/PROBE_O58_2026-09-08.md` → succeeded.
- Conclusion: **frontmatter hooks are not honoured for sub-agents.**

## Probe B — project-level hook, old guard in the worktree (agent counsel-quant-validation, worktree at d1ccb21; parent settings hook `python3 scripts/agent_guard.py`)
- Both writes **blocked** with `PreToolUse:Write hook error: [python3 scripts/agent_guard.py]: usage: agent_guard [-h] --agent AGENT ... error: the following arguments are required: --agent` (the worktree carried the pre-fix guard that required `--agent`).
- Conclusion: the **project-level hook fires for sub-agents**; the guard must obtain the identity from the payload.

## Probe C — payload identity (agent counsel-product-economics, worktree at 7b11f79 with the payload-aware guard; a temporary second hook logged the raw PreToolUse payload)
- Raw payload keys logged (verbatim, tool_input elided): `session_id, transcript_path, cwd, scratchpad_dir, prompt_id, permission_mode, agent_id, agent_type = "counsel-product-economics", effort, hook_event_name = "PreToolUse", tool_name = "Write", tool_input, tool_use_id`.
- In-scope write succeeded (guard allowed `docs/SESSIONS/` for a counsellor).
- Guard unit checks at the same commit: payload with `agent_type: counsel-ai-safety` → `docs/SESSIONS/x.md` exit 0, `services/risk/x.py` exit 2; payload without an agent field (main session) → exit 0. These cases are part of TC-AGT-001.

## Conclusion
Write/Edit calls of every sub-agent are inspected by the project-level hook with the harness-supplied `agent_type`; the roster is enforced for those tools. Not covered: Bash-driven file writes (R-46, R-54) — segregation of record therefore rests on authorship review, CODEOWNERS and the proposed CI authorship check (O-85). The temporary payload-logging hook was removed before commit.
