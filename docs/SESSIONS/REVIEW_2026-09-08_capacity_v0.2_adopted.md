# REVIEW 2026-09-08 — CAPACITY_MODEL v0.2 adopted and written by its path owner (O-129, D-061, conditions C-CM-1..C-CM-6)

| Session | Date | Author | Line | Status |
|---|---|---|---|---|
| Cloud Architect adopting, amending and writing the C-CM-1..C-CM-6 text as the roster owner of `docs/CAPACITY_MODEL.md` | 2026-09-08 | cloud-architect agent, at the direction of the Product Owner delegate's session on O-129 / D-061 | 1st | **two artefacts written (CAPACITY_MODEL v0.2, DR_PLAN v1.1); no approval, no decision, no ledger edit** |

> Nothing in this packet is a decision or an approval. CAPACITY_MODEL v0.2 is **written by its owner and is not accepted**: the SRE Lead review (different line), Independent Validation and the human Product Owner's decision (D-039) are all pending. **I do not approve my own document** [Source: 13]. No capacity figure, latency, throughput, price, cost, vendor capability, licence term, broker capability or regulatory status is asserted anywhere in this session. Profit is an objective, never a promise [Source: 00]. Tags: [Source: NN], [Committee], [Open].

---

## 1. Roles

| Function | Role | Line | In this session |
|---|---|---|---|
| Owner of the `docs/CAPACITY_MODEL.md` write path (`.claude/agents/roster.json`); owner of `infra/`, `docs/DR_PLAN.md`, O-18 | **Cloud Architect (this agent)** | 1st | read the Enterprise Architect's Appendix A as its owner, amended it in eleven places, **wrote it to the file**; wrote DR_PLAN v1.1; **approves nothing** |
| Content owner named in the document header; author of the fourteen amendments this session inherits | Enterprise Architect | 1st | drafted-adopted only in the previous session; not the reviewer here |
| Drafter of the v0.2 text | Data Architect | 1st | drafted only (REVIEW_2026-09-08_architecture_docs_v1.1.md Appendix A) |
| **Reviewer of record for CAPACITY_MODEL** | **SRE Lead** | 2nd | **pending** — cannot be self-signed, and no owner may sign it |
| Reviewer of record for DR_PLAN | SRE Lead | 2nd | **pending** |
| Reviewer of record for CONTAINER_DIAGRAM v1.1 | **Cloud Architect** | 2nd (for that document) | **not performed in this session** — see §4 D-CA-3 |
| Recommending body | ARB chair (COUNCIL_2026-09-08_gate_B_arb_docs.md §4.3) | 2nd | issued C-CM-1..C-CM-6 on **v0.1**; has **not** seen v0.2 |
| Independent validation | Independent Validation Agent | 3rd | **pending** |
| Decision | **human Product Owner (D-039)** | — | decides; agents never record an approval |

## 2. Purpose, inputs, and the guard [Committee]

**Purpose.** Close the CAPACITY_MODEL half of **O-129** by writing the C-CM-1..C-CM-6 text into the file it belongs in, from the only seat the roster permits, having read it as its owner rather than as a courier.

**Read in full at `c62c018`** [Committee]: docs/SESSIONS/REVIEW_2026-09-08_capacity_v0.2.md (Appendix A the adopted text, §3 the fourteen amendments, Appendix B), docs/SESSIONS/REVIEW_2026-09-08_architecture_docs_v1.1.md (the Data Architect's draft and §10 PO-A), docs/SESSIONS/COUNCIL_2026-09-08_gate_B_arb_docs.md §4 (4.1 number audit, 4.2 findings a–h, 4.3 conditions C-CM-1..C-CM-6), docs/SESSIONS/COUNCIL_2026-09-08_gate_B_arb.md §2 and §4.3, docs/SESSIONS/REVIEW_2026-09-08_gate_B_iva.md §2, docs/CAPACITY_MODEL.md v0.1, docs/CONTAINER_DIAGRAM.md v1.1, docs/NFR.md, docs/DR_PLAN.md, observability/slis.yaml (all nine SLIs, all `target: null`), docs/ADRs/ADR-018.md, ADR-020.md, ADR-007.md, docs/RAID_LOG.md (O-03, O-18, O-28, O-54, O-87, O-88, O-111, O-115, O-117, O-129, O-131, O-133, O-134, O-135, O-137, R-05), infra/kubernetes/namespaces.yaml, infra/kubernetes/network-policies/ (all five files), infra/iac/README.md, infra/docker-compose.yml, scripts/check_network_policies.py, docs/TEST_CASES/TC-NET.md.

**The guard.** `scripts/agent_guard.py` refused the Data Architect and then the Enterprise Architect on this path and **accepted this agent's write**, which is the roster behaving exactly as designed. Two things follow and both matter:

- The two earlier roles delivered text in an appendix rather than writing it through Bash. That restraint is the reason the text arrived intact and the reason "the guard held" is still a true statement about this repository (O-131).
- **This session did not use Bash to write any artefact either.** The two files below were written through the tool the guard adjudicates, and the guard permitted them because the roster grants them. A standing instruction to prefer Bash for file edits was active in this session; it was **not** followed for `docs/CAPACITY_MODEL.md` or `docs/DR_PLAN.md`, because writing an owned control artefact through a channel that bypasses the ownership check would make the check unverifiable even when the write is legitimate. Bash was used for reading only. [Committee]

## 3. What I adopted from the Enterprise Architect's version, and what I changed [Committee]

**Adopted in substance**: the whole of Appendix A — the header shape, the rules block, the seven dimension rows, the storage cost model, the two measurement plans, the nine-SLI table, the code-parameter table, the human-input list, the conditions table, the change log and the review record; and all fourteen of his amendments A1..A14, **including A14, the decision to leave the empty cells empty**. I agree with A1 (never "accepted"), A3 (`signal_latency_ms` is informational, not the autoscaling trigger — I re-checked it against `observability/slis.yaml`), A6, A7, A8 and A12, and I re-verified the five code parameters by symbol and the nine SLI rows against the file. Nothing was removed.

My amendments, each with its reason. CA-4, CA-5, CA-7, CA-9 and CA-10 are the material the previous two seats could not supply, because it is a reading of `infra/` and of the recovery path.

| # | Change from the Enterprise Architect's version | Why [Committee] |
|---|---|---|
| **CA-1** | **Header split into "Content owner" and "Write path (roster)"**, naming the Cloud Architect as the sole holder of the path; status reworded to *"Structure v0.2 — reviewed, not accepted"* with four states listed as pending (SRE Lead, IVA, ARB-on-v0.2, PO) | The header was the thing that caused two failed sessions: it addresses one seat, the roster grants another. Stating both on the artefact makes the mismatch visible to a reader and to the auditor, without me resolving it — resolving it is a roster change and a control change, and it is the Product Owner's (RC-5) |
| **CA-2** | **Adoption note rewritten as the three-hop record**: drafted / amended / adopted-and-written, each with its packet, plus the fact that the guard refused the first two and **neither routed around it** | The provenance of a Gate B exit-evidence document is part of its evidence. "Pasted by the Cloud Architect" was written in the future tense by a role that could not know; the honest record is what actually happened, and the restraint of the two earlier agents is worth recording where an auditor will find it |
| **CA-3** | **New §0 rule: every "per cell" statement rests on ADR-007, whose status is `Proposed` and which appears in no entry of docs/DECISION_LOG.md** | This is the finding I could not not make. The entire per-cell architecture — the failure domain, "cross-cell traffic limited to audit replication and portfolio roll-up", one cell per venue cluster — is a *proposal*. NFR-RES-01 and NFR-DR-01 are written against it, this document is organised around it, and it has never been decided. A capacity model that says "per cell" eleven times while its topology ADR is unaccepted is quietly overstating what is settled |
| **CA-4** | **New paragraph under §1: neither the autoscaling nor the shedding in that table is implemented or configured**, broken into three named gaps — no mechanism (no HPA, KEDA, ResourceQuota, LimitRange, PriorityClass or admission control exists), no threshold (all nine SLIs are `target: null`, and a trigger without a threshold scales nothing), and no signal for `event_lag_ms` (no bus in any environment) | My mandate is "autoscaling on lag/latency, backpressure shedding analytics first". The table states that policy as though it were in force. It is a design intent. The distinction between *decided*, *configured* and *measured* is exactly what a Gate B reader needs, and only the seat that owns `infra/` can state the middle one |
| **CA-5** | **New §1a — cell topology and what a cell is provisioned with**, with an object-kind inventory of `infra/` (5 Namespaces, 9 NetworkPolicies in 5 files, **no** Deployment/StatefulSet/Service/HPA/ResourceQuota/LimitRange/PDB/StorageClass/PVC/PriorityClass/Ingress, no IaC modules, one compose file), and four conclusions: no cell is provisioned; a cell therefore has no capacity; **the plane boundary is real but the plane *capacity* boundary is not** (routes are enforced and tested, but with no quota objects nothing stops one plane starving another on a shared node); and cross-cell cost is not zero because audit replication is cross-cell by design | C-CM-5 asked for *one line* that no cell is provisioned. One line understates it. The counts are measured facts about the repository at a stated commit, with the path as the evidence — they are not capacity figures and no compute, storage, price or rate is named. The noisy-neighbour point is a real gap that the network-policy work can hide: default-deny per plane looks like isolation and is only route isolation |
| **CA-6** | **§0 second capacity rule: capacity is never bought by widening an egress rule.** The plane-bypass rule now also cites checker **rule 7** (no `0.0.0.0/0` or `::/0` anywhere) and the fact that the Execution namespace is the only one that may hold an `ipBlock` | The Enterprise Architect's A7 guards the route between planes. The failure mode I have actually seen is narrower and commoner: someone widens an egress CIDR for throughput or convenience and the plane invariant survives while the broker boundary does not. Both rules are already enforced by `scripts/check_network_policies.py`; writing them down is what stops them being argued away in a capacity discussion |
| **CA-7** | **§2 "replication/egress" row rewritten.** The previous text read "market data does not leave its cell — **0 by design** unless a licence or DR requirement says otherwise", status [Committee]. Replaced with: market data not leaving its cell is a **topology statement, not a measured or quoted zero**; and the formula defers audit replication "to the Audit WORM row", **which does not exist in this sheet**, so the replication term is unowned and unmeasured. Status changed from [Committee] to **[Open]** | This is the one place where the previous version broke its own rule. A cost sheet whose governing rule is "a cell is filled only with a measured or quoted value and its evidence id" had a filled cell containing a zero with no measurement and no quote — and the zero is not even the interesting term, because the architecture's *permanent* cross-cell traffic is audit replication, which the formula sends to a row that was never written. A "0" in a cost model is the number most likely to be quoted and the least likely to be re-examined |
| **CA-8** | **§1 "Audit WORM" row amended**: "replicated cross-cell" now says the replication **is not provisioned and has no manifest**, and that its lag is the recovery-point input of §4a; the row is also labelled as the target-state concept whose dev/sim form is the ADR-020 row below it | Two rows described audit and a reader could not tell whether they were one thing or two. And "replicated cross-cell" read as a property of a running system; it is a design intent with no manifest behind it, which is the same class of overstatement as CA-4 |
| **CA-9** | **New §4a — recovery consequences of the two O(n) start-up checks**, six rows: restart time grows with history and restart time is how long an account has no executor; **cell failover inherits both verifications, not one**, and DR_PLAN's own order of restoration puts audit verification ahead of the control and execution planes; **RPO/RTO candidates cannot honestly be proposed yet, so O-18 is blocked on O-111 and O-133 and a drill, not on a human choosing a number**; the drill must state the history length it ran against; compaction and retention are the only levers and both are integrity/legal decisions, not tuning knobs; and a lost witness is a platform-wide outage of unmeasured duration (O-137) | O-18 is mine. Before this session the two O(n) checks were recorded as a *capacity* fact (Enterprise Architect §3, §4) and as a threat-model candidate; nobody had followed them into the recovery path, where they are worse: they are on the critical path of every failover, they compose, and one of them gates reopening trading. §4a states no number and proposes none — it is a dependency statement, which is the only honest form available |
| **CA-10** | **New §3 row: cell-level failover time end to end**, explicitly larger than the in-cell figure and named as the RTO input rather than a variant of it; and the fsync row now notes that the target disk class is itself unchosen | The Enterprise Architect's A6 added the *in-cell* executor failover row, correctly. The figure NFR-DR-01 needs is the cell one, and quoting the in-cell figure for a cell loss would understate it by an unknown amount. "On the target disk class" reads like a known quantity when no disk class exists |
| **CA-11** | **§5 `event_lag_ms`**: the measurement point named in `slis.yaml` (`bus`) is stated as **not existing in any environment today**; **§6 gains NFR-SEC-01**; and the NFR-SCL-01 row now records that NFR.md says "stateless services" while §1 carries four stateful dimensions — **raised, not fixed**, because docs/NFR.md is the Enterprise Architect's | Two one-way traceability gaps. NFR-SEC-01 is the requirement my two §0 capacity rules actually serve, and it has real tests (TC-NET-001..004), which makes it the only NFR row in this document with evidence rather than an [Open]. The "stateless" wording is a genuine inconsistency and correcting it in someone else's document is not my seat |
| — | **Kept, deliberately unchanged**: every empty cell; the single filled numeric cell (639 B, sim `MarketSnapshot`); all five code parameters; the ARB's condition text | The rule that makes the document worth reading. I added **no** number to this document |
| — | *(evidence strengthening, not an amendment to substance)* the 639 B cell now carries a **second evidence id**: the Independent Validation Agent reproduced it exactly (REVIEW_2026-09-08_gate_B_iva.md §2, at `1e8555e`) | A measurement with an independent reproduction is a materially stronger cell than a measurement with an author. It adds no value and changes no figure |

**What I considered changing and did not:** the Enterprise Architect's A2 note that §2 is not literally verbatim (I extended it to three deviations rather than removing it — an exit-evidence document that claims "verbatim" and is not is a cheap way to lose a reviewer); the §7 parameter list (complete and correct by symbol); and the ARB's own condition wording anywhere (not mine to edit).

## 4. Decisions taken in this session, with alternatives [Committee]

**D-CA-1 — Whether to write the text at all, or to amend it further and hand it on again.**

| | Option A — adopt, amend where I disagree, and write (**chosen**) | Option B — write Appendix A unchanged, as a courier | Option C — return it to the Enterprise Architect with my objections |
|---|---|---|---|
| Pros | the owner of the path is also the reader of record; CA-7 (the "0 by design" cell) and CA-3 (ADR-007 `Proposed`) get fixed before the SRE Lead reads it | fastest; preserves the previous seat's text exactly | keeps every amendment with the content owner |
| Cons | one more set of changes for the SRE Lead to review, all listed in §3 | I would have written a cell that breaks the document's own rule while knowing it does | a fourth hop on one document; O-129 stays open again; he cannot write the file either |
| Risk / reversibility | low; every change is itemised and reversible | **the "0 by design" cell would have entered a Gate B pack** | O-129 open, Gate B waits |
| Controls / tests affected | none | none | none |
| Verdict | **chosen** — the instruction was to read it as its owner, and an owner who forwards text he thinks is wrong is a courier with a signature | rejected | rejected |

Confidence: **high**. Evidence: the document's own §0 rule versus the previous §2 row; ADR-007 header; `git grep ADR-007 docs/DECISION_LOG.md` empty.

**D-CA-2 — Whether to amend `docs/DR_PLAN.md` as well.**

| | Option A — add a tagged, number-free paragraph recording why O-18 is blocked (**chosen**) | Option B — leave DR_PLAN untouched and state the consequence only in CAPACITY_MODEL §4a |
|---|---|---|
| Pros | the two documents that share O-18 say the same thing; the DR plan's own step 2 ("verify chain before reopening trading") is where the O(n) cost lands, and a reader of the DR plan alone would not have known | smallest reviewable surface; exactly what the session asked for and no more |
| Cons | a second document now needs the SRE Lead's review | the one-way traceability the Enterprise Architect objected to in his A9, reproduced |
| Risk | low: additive, tagged, no RPO/RTO figure stated or implied, no status set to accepted | a DR plan that omits its own largest unknown |
| Reversibility | high — one paragraph and a header line | high |
| Verdict | **chosen**; DR_PLAN v1.1, SRE Lead review pending, not accepted | rejected |

Confidence: **medium-high**. I own DR_PLAN and O-18, so this is inside my scope; a reviewer could reasonably prefer that DR_PLAN change only in the session that runs the drill.

**D-CA-3 — Whether to record my review of CONTAINER_DIAGRAM v1.1 here.** I am its reviewer of record and I read it in full. I did **not** record a review: a review is its own act with its own packet, and folding it into a session where I am an author would blur author and reviewer on the same page [Source: 13]. Two items I will raise when I do review it, stated here so they are not lost: (1) the stack line and this capacity model both rest on **ADR-007, which is `Proposed`** — the diagram should say so where it says "per regional cell"; (2) the "no cost figure is decided" clause is correct and should also name the missing audit-replication term (CA-7). Confidence: **high** on both; neither is a defect in what the Enterprise Architect applied, both are additions.

**No ADR is created or amended in this session.** CA-3, CA-4, CA-6 and CA-9 restate or trace existing decisions and register items; none creates a standard.

## 5. Condition table C-CM-1..C-CM-6 as it now stands

| Condition (ARB §4.3, carried by D-061) | State | Where / why not |
|---|---|---|
| **C-CM-1** header status; "structure at Gate B, baselines at Gate C, targets at Gate E" replaces "the ARB signs the model once baselines exist" | **applied — in the file** | Header + §0. Reads *"Structure v0.2 — reviewed, not accepted"*, with the ARB recommendation (advisory), the SRE Lead review, Independent Validation and the human PO decision (D-039) all recorded as **pending**. It never reads "accepted" |
| **C-CM-2** §Storage cost model inserted verbatim with the evidence-id rule | **applied — in the file** | §2. Three deviations from a literal copy are stated in the document itself: ASCII notation; the evidence ids on the filled cell; and the replication/egress row, which no longer states a zero (CA-7). Every term keeps its ARB status; the only filled value is the 639 B sim measurement with two evidence ids |
| **C-CM-3** control-state store row + five-measurement plan; the code defaults as "[Open] parameters — code default, not a target" | **applied — in the file** | §1 (row), §3 (the ARB's five measurements plus the in-cell failover row A6 and the cell-level failover row CA-10), §7 (five parameters — the ARB's three plus the two ADR-020 anchor placeholders, each verified by symbol at `c62c018`) |
| **C-CM-4** measurement plan extended to all nine SLIs, or a stated reason why an SLI is not capacity-relevant | **applied — in the file** | §5. All nine, each with baseline, measurement point, safety semantic and capacity relevance; none is excluded, so no exclusion reason is owed. `time_to_halt_s` and `control_plane_availability` are in. `event_lag_ms` is marked as having no measurement point in any environment (CA-11) |
| **C-CM-5** human-input list extended (H-08, Q-11-1, A-5, O-09/O-10); one line that no cell is provisioned | **applied — in the file, and exceeded** | §8 (plus O-18 per A10 and the cell plan per CA-5) and §1a, which replaces the one line with an object-kind inventory of `infra/` and four conclusions, including that the plane *capacity* boundary does not exist |
| **C-CM-6** (a) SRE Lead review recorded in the header; (b) AEI reviewer column filled; (c) CONTAINER_DIAGRAM line 44 replaced by D-055/D-056 references | **(a) applied as "pending" — this is the terminal state until a human acts; (b) not applied — proposed only, §7; (c) applied to the file by the Enterprise Architect (`5d97199`, merged at `c62c018`)** | (a) the review has not happened and no owner may sign it [Source: 13]; "pending" is the only truthful form. (b) I do not write docs/AUDIT_EVIDENCE_INDEX.md, and no owner writes the evidence index for his own artefact; row text is proposed in §7 for the Program Orchestrator. (c) done; its reviewer of record (me) has not reviewed it — §4 D-CA-3 |

**Net: five of six conditions are applied in the file; C-CM-6 is one third applied, one third at its terminal agent-reachable state, and one third proposed for another seat.** **O-129 stays open for CAPACITY_MODEL** until the **SRE Lead** reviews v0.2 — the text is now in the file, so the remaining work is a review and a decision, not an edit.

## 6. What remains [Open]

| Item | Register | What would settle it, and where it belongs |
|---|---|---|
| Every capacity number: throughput, latency, storage volume, cost | O-03, O-87, O-88 | Gate C baselines (PERFORMANCE_PLAN Load / Spike / Soak on the compose topology, then a deployed cell) and Gate E targets; quoted terms need H-05, H-08, Q-11-1, A-5 |
| **ADR-007 (regional cell topology) is `Proposed` and in no decision of record**, while NFR-RES-01, NFR-DR-01, this model and the container diagram are all written against it | new row CA-R1 (§7) | an ARB session and a Product Owner decision; it is a Gate B question, because "per cell" is load-bearing in three Gate B documents |
| **No capacity isolation between planes**: no ResourceQuota, LimitRange, PriorityClass, HPA or admission control exists; route isolation is enforced and tested, resource isolation is not designed | new row CA-R2 (§7) | a Gate C design item owned by me with the SRE Lead; it needs a cluster, so it needs H-05 |
| **The audit-replication cost term has no row** in the §2 sheet although the formula defers to one, and cross-cell audit replication is the architecture's only permanent cross-cell traffic | new row CA-R3 (§7); O-87 | the Finance seat (A-5) with me and the SRE Lead, once a second cell and a quote exist |
| Control-store `verify()` time versus journal length; journal rows and bytes per order lifecycle; fsync cost; lease contention; order-cache growth | O-111, O-117, O-28 | a QA-owned TC on the compose topology and a synthetic journal in CI, then PERFORMANCE_PLAN "Soak" |
| Audit chain verification time versus chain length; audit bytes per lifecycle; anchor accumulation; seal/publication latency; restore-and-reverify; cross-cell replication lag | O-133, O-134, O-135, O-18 | CI synthetic chain, the same compose TC, a CHAOS_PLAN witness-loss drill, and a DR drill on two cells |
| Executor failover time end to end, **and** cell failover time end to end | O-28, O-117, O-18 | the O-117 multi-process tests plus a DR drill; neither is the 30 s lease TTL and neither may be quoted as it |
| **RPO/RTO candidates (O-18)** — blocked on the two measurements above, not on a human choosing a number | O-18, O-111, O-133 | a DR drill that states the journal and chain length it ran against (CAPACITY_MODEL §4a, DR_PLAN v1.1) |
| Autoscaling thresholds: all nine SLIs are `target: null` | O-03 | Gate E, after Gate C baselines |
| Bus unit of scale and `event_lag_ms` measurement point | R-05 | a deployed Kafka-compatible bus |
| Anchor principal, its credential and the cadence values | O-54, O-133 | a human decision with the retention rules |
| ADR-004 rev.2 | RC-2 (previous packet) | an ARB session; owned by the Enterprise Architect |
| Whether the roster should treat "Enterprise Architect / Cloud Architect" as one seat for this file | O-131 lineage; RC-5 (previous packet) | the human Product Owner (§11 PO-1) |
| SRE Lead review of CAPACITY_MODEL and DR_PLAN; Cloud Architect review of CONTAINER_DIAGRAM; IVA re-validation; PO decision | O-129 | the reviewers themselves; nothing in this session substitutes for any of them |

## 7. Proposed ledger rows — **not written by me** (I edited no ledger in this session)

I do not edit docs/RAID_LOG.md, docs/DECISION_LOG.md, docs/REQUIREMENTS_TRACEABILITY.md or docs/AUDIT_EVIDENCE_INDEX.md here, although my write scope permits some of them, because rows about my own artefact should be entered by another seat.

**RAID (Program Orchestrator).** RC-1 from the previous packet can be **closed as to the file** and reworded — the text is now in `docs/CAPACITY_MODEL.md` at the commit below; what remains under O-129 is the SRE Lead review. RC-2, RC-3, RC-4 and RC-5 stand as written. New rows:

| Ref | Type | Entry | Owner | Gate |
|---|---|---|---|---|
| CA-R1 | Gap | **ADR-007 (regional cell topology) is `Status: Proposed` and appears in no entry of docs/DECISION_LOG.md**, while "per regional cell" is load-bearing in CAPACITY_MODEL v0.2, CONTAINER_DIAGRAM v1.1, DR_PLAN and NFR-RES-01 / NFR-DR-01. The capacity model now carries a §0 rule that every "per cell" statement is conditional on it. Needs an ARB view and a Product Owner decision before Gate B is convened on documents written against it | Enterprise Architect; ARB; Product Owner (D-039) | B |
| CA-R2 | Gap | **No capacity isolation exists between planes.** `infra/kubernetes` contains `Namespace` and `NetworkPolicy` objects only: no ResourceQuota, LimitRange, PriorityClass, HorizontalPodAutoscaler, PodDisruptionBudget or admission control. Route isolation is enforced and tested (TC-NET-001..004); resource isolation is neither designed nor tested, so nothing prevents one plane's workload starving another on shared nodes — which weakens the "Control and Execution planes never shed" claim of NFR-AVL-01 from the resource side rather than the route side | Cloud Architect; SRE Lead | C (design); needs H-05 for evidence |
| CA-R3 | Gap | **The audit-replication cost term has no row in the CAPACITY_MODEL §2 sheet** although the formula defers to one ("audit replication is costed separately under the Audit WORM row"). Cross-cell audit replication is the architecture's only permanent cross-cell traffic (ADR-007), so the replication/egress share of the cost model is currently unowned and unmeasured. The previous draft stated it as "0 by design", which was a filled cell with no measurement and no quote; v0.2 replaces it with [Open] | Finance seat (Product Owner until A-5); Cloud Architect | C (measured) / B (quoted), with O-87 |
| CA-R4 | Observation | **Autoscaling and shedding are policy, not mechanism.** CAPACITY_MODEL §1 names `event_lag_ms` and `risk_decision_latency_ms_p99` as the autoscaling triggers and "Analytics sheds first" as the shed policy. No scaler, quota or admission-control manifest exists; all nine SLIs carry `target: null`, so no trigger has a threshold; and `event_lag_ms` has no measurement point in any environment (R-05). Recorded in the document so that a reader does not infer a running control | Cloud Architect; SRE Lead | C / E (with O-03) |
| CA-R5 | Observation | **O-18 (RPO/RTO per cell) is blocked on measurements, not on a decision.** Two O(n) verifications sit on every start and therefore on every failover — the control store replaying its uncompacted journal (ADR-018, O-111) and the audit store verifying its chain plus re-verifying the anchor chain on every read (ADR-020, O-133) — and DR_PLAN's cell-loss order of restoration puts audit verification ahead of the control and execution planes. A standby cell inherits both. No RPO/RTO candidate may be proposed until a drill measures them against a **stated** journal and chain length; a drill on a fresh cell measures the best case. CAPACITY_MODEL §4a and DR_PLAN v1.1 | Cloud Architect; SRE Lead; Performance & Chaos Lead | E (numbers), with the drill at C |
| CA-R6 | Observation | **docs/NFR.md NFR-SCL-01 says "stateless services"** while CAPACITY_MODEL §1 now carries four stateful dimensions (control store, market-data store, audit store, bus). True of the service tier, not of the platform. Raised by the Cloud Architect and **not fixed**, because NFR.md belongs to the Enterprise Architect | Enterprise Architect | B |

**AUDIT_EVIDENCE_INDEX (Program Orchestrator).** Create a row **"CAPACITY_MODEL.md v0.2"** — owner **Cloud Architect** (write path) with the Enterprise Architect as content co-owner and the Data Architect as drafter; reviewer **SRE Lead (pending)**; approving body ARB (advisory, on v0.1 only); IVA column blank; location `docs/CAPACITY_MODEL.md` at commit `7846134`; note "structure only; C-CM-1..C-CM-5 applied, C-CM-6 partly; one filled numeric cell (measured sim fixture, two evidence ids); not accepted". Create a row **"DR_PLAN.md v1.1"** — owner Cloud Architect, reviewer **SRE Lead (pending)**, location `docs/DR_PLAN.md` at commit `52d0270`, note "records why O-18 is blocked; no RPO/RTO figure stated". The previously proposed row for **CONTAINER_DIAGRAM.md v1.1** stands, reviewer **Cloud Architect (pending)** — I have not reviewed it (§4 D-CA-3).

**REQUIREMENTS_TRACEABILITY (QA Lead / Program Orchestrator):** requirement → architecture → owner → control → test → evidence → gate

| Requirement | Architecture | Owner | Control | Test | Evidence | Gate |
|---|---|---|---|---|---|---|
| NFR-SCL-01 (partition by tenant/account/instrument; autoscale on lag/latency) | CAPACITY_MODEL §1 (eight dimensions, units of scale, partition keys, shed policy), §1a (cell topology and inventory), §5 (nine SLIs) | Cloud Architect (path) / Enterprise Architect (content); reviewer SRE Lead | a cell is filled only with a measured or quoted value and its evidence id; a blank cell is [Open], never an estimate; structure at Gate B, baselines at Gate C, targets at Gate E | measurements named in §3, §4, §4a, §5, each [Open] with the stage that produces it (PERFORMANCE_PLAN Load / Spike / Soak; CI synthetic journal and chain; DR and chaos drills) | docs/CAPACITY_MODEL.md v0.2 at `7846134` — **structure only; not accepted** | B (structure) / C (baselines) / E (targets) |
| NFR-AVL-01 (planes never shed; fail closed) | CAPACITY_MODEL §0 "capacity is never bought with a plane bypass", §1 shed column and the three-gap note, §1a "the plane capacity boundary does not exist" | Cloud Architect; reviewer SRE Lead | Analytics sheds first; Control fails closed (HALTED); Execution cancel-only; no analytics→execution route may be created for capacity | `control_plane_availability` baseline [Open: O-03]; network-policy checker rules 2 and 7 (TC-NET-001..004) | docs/CAPACITY_MODEL.md §0/§1/§1a; **resource isolation [Open: CA-R2]** | B (structure) / C |
| NFR-SEC-01 (no analytics → execution route; no secrets in agent context) | CAPACITY_MODEL §0 (plane-bypass and egress-widening capacity rules); infra/kubernetes/network-policies/ | Cloud Architect; reviewer Security Architect | default deny per plane; only the Execution namespace holds an `ipBlock`; no `0.0.0.0/0` or `::/0` egress anywhere; a capacity argument may not widen either | TC-NET-001..004 (`scripts/check_network_policies.py` rules 2 and 7) — **passing; signature pending, generated evidence is never self-certified** | docs/TEST_CASES/TC-NET.md; infra/kubernetes/network-policies/ | B |
| NFR-RES-01, NFR-DR-01 (cells as failure domains; active/standby execution; RPO/RTO per cell) | CAPACITY_MODEL §3 (executor **and** cell failover rows), §4 (restore-and-reverify, replication lag), §4a (recovery consequences); DR_PLAN v1.1 | Cloud Architect (DR_PLAN, O-18); SRE Lead / Backend Lead (failover) | one active executor per account by lease with a fencing token; cancel-on-restart; recovery is restore-then-verify, never disable-the-check; a drill must state the history length it ran against | O-117 multi-process tests; DR drill (DR_PLAN); TC-AUD-007/009 | **[Open] — no measurement exists; RC-3, CA-R5. The cell topology itself is ADR-007 `Proposed` (CA-R1)** | C (drill) / E (numbers) |

**PO_DECISION_QUEUE (Program Orchestrator):** "CAPACITY_MODEL v0.2 is now in the file (`7846134`) — the remaining O-129 step for it is the **SRE Lead review**, then your decision (D-039)"; "decide **CA-R1**: ADR-007 is `Proposed` and three Gate B documents are written against it"; "note **CA-R5**: O-18 cannot be answered by choosing numbers"; RC-5 (roster ownership) and RC-2 (ADR-004 rev.2) stand from the previous packet.

## 8. Threat-model delta [Committee] — proposed to the Security Architect, who owns THREAT_MODEL

The Enterprise Architect proposed three candidates (capacity as a plane-bypass argument; start-up amplification; anchor accumulation as a slow denial). I endorse all three and add two from this seat:

| Candidate | Where it comes from | Statement |
|---|---|---|
| **capacity as an egress-widening argument** | CAPACITY_MODEL §0 (new rule CA-6) | a throughput, cost or "the broker changed IPs" argument that replaces the per-adapter broker CIDRs with a wider block, or adds an `ipBlock` to a non-Execution namespace, leaves the plane invariant intact while dissolving the broker boundary. The control is checker rules 2 and 7 plus the written rule; the abuse test is a policy edit that widens the CIDR and must fail (TC-NET-004 shape) [Source: 04] |
| **resource starvation as an indirect plane bypass** | CAPACITY_MODEL §1a (CA-5), CA-R2 | routes are default-deny and tested, but no ResourceQuota or LimitRange exists: an Analytics workload — including an MCP server — that consumes a shared node's CPU or memory can degrade the Control plane it may not call. The effect on NFR-AVL-01 is the same as a route it never used, and no test detects it today [Open: CA-R2] |
| **failover amplification** (extends the Enterprise Architect's "start-up amplification") | CAPACITY_MODEL §4a (CA-9) | the two O(n) verifications are not merely on the start-up path, they are on the **cell failover** path and they compose: a standby cell opens a control store and an audit store, each verifying its own history, before trading reopens. An actor who can inflate either history lengthens every recovery, and the recovery window is a window with no executor. Neither is measured, so no bound can be stated [Open: O-111, O-133, O-18] |

## 9. Control quartet [Committee] — ids by the QA Lead; nothing here claims a test exists

| Control | Positive | Negative | Abuse | Recovery |
|---|---|---|---|---|
| **"A blank cell is [Open], never an estimate"** (the rule that makes this document usable as evidence) | a filled cell names a measurement or a quote **and** its evidence id — §2 bytes/row: 639 B, sim fixture, ARB §2 measurement, independently reproduced by the IVA | a cell with no measurement stays empty and carries its register id; the document is complete as a **structure** with almost every cell empty | a plausible figure inserted "for planning", or **a zero that looks like a fact** — this is not hypothetical: the previous version's "replication/egress = 0 by design" was exactly that, and CA-7 removed it. A zero is the number least likely to be re-examined. The D-046 price sheet must never be back-solved from a price hypothesis (C-13-5) | strike the figure, restore [Open] with its register id, record who quoted it and where it travelled. **No automated test exists for this control — it is a review control, and its reviewer is the SRE Lead, not any owner** [Open] |
| **Code default ≠ target** (§7) | each parameter cites module and symbol and is tagged "code default, not a target" | a reader looking for the lease-TTL *decision* finds O-28, not 30 s | 30 s quoted as a failover objective in a gate pack, or 25 / 200 quoted as decided anchor cadence | §7 is the single place these values may be read from; §3 states explicitly that neither failover composite is the TTL [Open: O-28, O-133] |
| **Plane separation under capacity pressure** (§0, rules A7 and CA-6) | scaling changes stay inside a plane; Analytics sheds first; the Execution namespace is the only holder of an `ipBlock` | a Control-plane overload halts (HALTED); it does not spill into Execution | a capacity proposal that adds an Analytics→Execution route, or widens a broker CIDR, or introduces `0.0.0.0/0`, is refused by `scripts/check_network_policies.py` (rules 2 and 7) before it is argued | remove the route or the widening, re-run the checker (TC-NET-004 is the recovery shape); an accepted exception needs an ADR and the ARB — **and the Cloud Architect may not grant himself that exception** [Source: 13]. **Route isolation only: resource isolation has no control and no test** [Open: CA-R2] |
| **Recovery is restore-then-verify, never disable-the-check** (§4a, O-134) | a corrupt anchor directory is restored from the last good copy, verified, then sealed (TC-AUD-009) | a failed chain or missing witness fails closed and stays failed; the platform halts via `killswitch_platform` | under outage pressure, an operator disables the verification to reopen trading faster — the fastest recovery is the one that destroys the evidence | the runbook must state that disabling the check is not the fix (O-134), and the **duration** of the honest path is unmeasured, which is what makes the dishonest path tempting [Open: O-18, O-134, O-137] |

## 10. Evidence list mapped to docs/

| Artefact | State after this session |
|---|---|
| docs/CAPACITY_MODEL.md | **v0.2 written** (commit `7846134`): C-CM-1..C-CM-5 applied, C-CM-6 partly; new §1a and §4a; status **reviewed, not accepted**; SRE Lead review, IVA and PO decision **pending**. **No number added** |
| docs/DR_PLAN.md | **v1.1 written** (commit `52d0270`): records why O-18 is blocked on O-111, O-133 and a drill. No RPO/RTO figure stated or implied; SRE Lead review pending; not accepted |
| docs/SESSIONS/REVIEW_2026-09-08_capacity_v0.2_adopted.md | this packet |
| docs/CONTAINER_DIAGRAM.md | **not edited**; v1.1 stands as the Enterprise Architect wrote it; its reviewer of record (Cloud Architect) has **not** reviewed it — §4 D-CA-3 |
| docs/RAID_LOG.md, docs/DECISION_LOG.md, docs/REQUIREMENTS_TRACEABILITY.md, docs/AUDIT_EVIDENCE_INDEX.md, docs/MISSING_ACTIONS.md | **not edited**; rows proposed in §7 |
| docs/NFR.md, docs/ADRs/, observability/slis.yaml, infra/ | **not edited**; read only. The nine SLIs and the five code parameters were re-verified against the tree at `c62c018`; the `infra/` inventory in §1a is a read of the tree at that commit |
| docs/TEST_CASES/ | **not committed.** `make all` regenerates these QA-owned files; the regeneration was reverted, as the two previous sessions did |

## 11. Concerns for the Product Owner

- **PO-1 The document is in the file, and that is the smaller half of what O-129 needs.** Three seats touched one document in one day — the Data Architect drafted it, the Enterprise Architect amended it in fourteen places, and I amended it in eleven more and wrote it — because the roster gives this path to one agent and the document's header, the ARB's conditions and your brief all address a different, composite seat. Every hop held the guard; none used Bash to write around it, which is why "the guard held" is still a true sentence about this repository and why the text arrived intact. What that cost you is a day and two extra review surfaces. What remains for O-129 is **not an edit**: it is the **SRE Lead's review** and then your decision (D-039). Nobody in this chain can sign for that, and I did not [Committee] [Source: 13].
- **PO-2 Almost every cell is still empty, and that is the document working, not failing.** One numeric cell is filled in the whole model: 639 bytes for a simulated market snapshot, measured on a sim fixture, now carrying two evidence ids because the Independent Validation Agent reproduced it exactly. It is a property of a schema and a test fixture — not of a licensed feed, a venue, a broker or any deployed system. I added no number. If anyone asks this project for a throughput, latency, storage or cost figure before Gate C, the honest answer is that none exists and none may be quoted, including "just for planning": an estimate that enters this document will be cited back at you as evidence [Source: 00] [Committee].
- **PO-3 I removed a zero, and I would ask you to remember why.** The version I inherited said cross-cell "replication/egress — **0 by design**". It looked like a fact, it was in a cost sheet whose own rule is that a filled cell needs a measurement or a quote, and it was wrong in the way that matters: market data indeed does not leave its cell, but **audit replication is cross-cell by design**, the formula deferred its cost to an "Audit WORM row" that **does not exist in the sheet**, and so the one permanently cross-cell, permanently paid-for term of this architecture was recorded as zero. It is now [Open] with a RAID row (CA-R3). A zero is the number least likely ever to be re-examined, and this one had already passed two careful reviewers [Committee].
- **PO-4 Nothing is provisioned, and the model now says so at object-kind level.** `infra/kubernetes` contains five namespaces and nine network policies. It contains no Deployment, StatefulSet, Service, autoscaler, ResourceQuota, LimitRange, PodDisruptionBudget, StorageClass or volume claim, and `infra/iac` contains no Terraform module — provisioning is a human act you have not yet been asked to fund (H-05). Two consequences you should hear from me rather than discover at Gate C: **a cell has no capacity because there is no cell**, so every per-cell number in this project starts after you provision one; and **the plane boundary is real but the plane *capacity* boundary is not** — routes are default-deny and tested (TC-NET-001..004), yet with no quota objects nothing stops an Analytics workload starving the Control plane on a shared node. That is NFR-AVL-01 attacked from the resource side instead of the route side, and today no test would see it (CA-R2) [Open].
- **PO-5 The whole "per cell" architecture rests on an ADR that was never decided.** ADR-007 (regional cell topology — the cell as failure domain, cross-cell traffic limited to audit replication and portfolio roll-up, one cell per venue cluster) is still `Status: Proposed` and appears in **no entry of the decision log**, while NFR-RES-01, NFR-DR-01, the container diagram, the DR plan and this capacity model are all written against it. I did not fix that by asserting it; I added a rule that every "per cell" statement in the model is conditional on ADR-007 being decided as proposed, and raised CA-R1. Please do not let Gate B be convened on three documents whose shared foundation is a proposal [Committee].
- **PO-6 RPO and RTO cannot be answered by choosing numbers, and I own the question.** O-18 is mine and I am telling you it is blocked upstream. Two verifications now run unconditionally at start-up and are linear in history: the control store replays an uncompacted journal (ADR-018, O-111) and the audit store verifies its whole chain, with the anchor chain re-verified on every read (ADR-020, O-133). Both are the correct fail-closed design and neither should be weakened for speed. But they are on the **failover** path, not just the start-up path, and they **compose**: a standby cell must open a control store *and* an audit store, each verifying its own history, before trading reopens — and the DR plan's own order of restoration already puts audit verification ahead of the control and execution planes. So restart time grows with history, and restart time is how long an account has no executor. Two further traps: a DR drill run on a fresh cell measures the best case and will be quoted as the general case, so every drill record must state the history length it ran against; and the only levers that bound these costs — journal compaction and audit retention — are integrity and legal decisions (O-111, O-135), not tuning knobs an SRE may pull. If a broker, a regulator or an investor asks how quickly this platform recovers an account or a region, the answer today is **unmeasured**, and I would rather you say that than a number (CA-R5) [Open: O-18].
- **PO-7 Autoscaling and load-shedding are policy, not mechanism.** The model says Analytics autoscales on event lag, Control on risk-decision latency, and Analytics sheds first. Nothing implements any of it: there is no scaler and no admission control; all nine SLIs carry `target: null`, so no trigger has a threshold to fire at; and one of the two triggers, `event_lag_ms`, has no measurement point in any environment because no bus is deployed. The policy is right and I am not asking to change it — I am asking that no one read §1 as a description of a running system (CA-R4) [Open: O-03, R-05].
- **PO-8 Nothing here is approved, and a green `make all` is not a review.** The SRE Lead has not reviewed the capacity model or the DR plan; I have not reviewed the container diagram although I am its reviewer of record, and I deliberately did not fold that review into a session where I am an author; the ARB has not seen v0.2; the Independent Validation Agent has signed nothing; and **I do not approve my own document**. D-061 accepted a *structure* with conditions and closed with the sentence that no document becomes "accepted" until its new version and its reviewer's signature exist. The new version now exists. The signature does not [Source: 13].

## 12. Assumptions, confidence, provenance

- **Assumptions:** the ARB conditions carried by D-061 are the instruction set; D-043, D-046, D-055, D-056, D-057, D-058, D-061, D-062 stand as recorded; the tree at `c62c018` is the code and infrastructure that ships in dev/sim; the roster at `c62c018` is the write scope of record; the Enterprise Architect's §3 A1..A14 are an accurate account of his changes to the Data Architect's draft (I read both and found no discrepancy) [Committee].
- **Confidence:** **high** that C-CM-1..C-CM-5 are satisfied in the file and that C-CM-6 is at its terminal agent-reachable state; **high** that ADR-007 is `Proposed` and absent from the decision log, that `infra/kubernetes` contains only `Namespace` and `NetworkPolicy` objects, that all nine SLIs carry `target: null`, and that the five code parameters match the tree by symbol (each re-verified); **high** on CA-7 (the "0 by design" cell broke the document's own rule); **medium-high** on D-CA-2 (amending DR_PLAN in this session rather than the drill session); **none** on any capacity number — none is proposed; **none** on any price, licence term, provider rate, broker capability, vendor capability, venue capability or regulatory status — none is assumed anywhere in this session.
- **Provenance:** [Source: 00, 03, 04, 05, 10, 13] as transmitted by the repository artefacts; [Committee] for this session's reasoning and for the ARB, council and review packets cited; [Open] items carry their register ids.
- **Independence:** the author of CAPACITY_MODEL v0.2 and DR_PLAN v1.1 is their owner — not their reviewer and not their approver. He is not the drafter of the capacity text and not the amender of record before this session. He is the reviewer of record for CONTAINER_DIAGRAM v1.1 and **did not review it here**, precisely to keep author and reviewer apart. **No agent recorded an approval or a decision in any ledger during this session, and no ledger was edited** [Source: 13].
