# THREAT_MODEL

| Author of record | Transcriber (rows from build/council packets) | Reviewer (different line) | Recommending body | Decider | First gate | Status |
|---|---|---|---|---|---|---|
| Security Architect | Product Owner delegate (T-38..T-79; see footnote) | Red-Team & Pen-Test Lead (3rd line) — signature pending, packet `docs/SESSIONS/REVIEW_<date>_threat_model_red_team.md` [Open: not yet written] | Security & Privacy Board (recommends; COUNCIL_2026-09-08_gate_B_spb_docs §1.7) | Product Owner (human, D-039; AI delegate under D-040) | B | v1.1 — reviewed, Board recommendation issued, decision pending |

Version note [Committee: COUNCIL_2026-09-08_gate_B_spb_docs §1.7; D-061 (1)]: v1.1 applies conditions TM-C1..TM-C12. No status of "accepted" is recorded here: D-061 states that no document status changes to accepted until its new version *and* its reviewer signature exist, and the reviewer signature does not exist at this commit. The document describes a **dev/sim** baseline and authorises nothing beyond dev/sim [Source: 12]. Profit is an objective, never a promise [Source: 00].

**Reading rule.** A test id tagged `[Open]` **does not exist** and is not evidence; it is an allocated identifier with a RAID reference. A control column that says "interim" describes what runs today in dev/sim; "target" describes what is designed and not built. No row claims a control that has no test without saying so.

## Trust boundaries [Committee; TM-C1]
B1 user↔BFF · B2 BFF↔services · B3 Analytics↔Control plane · B4 Control↔Execution plane · B5 Execution↔broker · B6 MCP server↔tool · B7 data provider↔ingest · B8 CI/agent harness↔repository/production · **B9 model gateway↔model provider** (D-052) · **B10 host/OS↔control-store, audit-store and journal files** (ADR-018, ADR-020) · **B11 platform↔regulatory adapters** (DATA_FLOWS DF-09).

B9, B10 and B11 are declared here in v1.1. Mirroring them in DATA_FLOWS (a flow for the model gateway, a host/file flow, residency notes on DF-01/DF-02) is owed by the Data Architect in DATA_FLOWS v1.1 [Open: O-129; ARB conditions C-DF-1..C-DF-6].

## Threats [Source: 06] → control → test → owner
| ID | Threat | Boundary | Control | Test ID | Owner |
|---|---|---|---|---|---|
| T-01 | Prompt/tool injection | B6 | Provenance labels, delimited untrusted text, allowlisted tools, output schema | TC-AI-002/003 | MCP Security Agent |
| T-02 | Excessive agency | B3, B6 | Write-class tool writes to the queue only; no broker route (see also T-28 for the in-process plane guard) | TC-NET-003 | MCP Security Agent |
| T-03 | Poisoned market data | B7 | Source contracts, outlier policy, cross-source check, freshness (see also T-88 for the entitlement boundary) | TC-MD-003 | Data Architect |
| T-04 | Model supply-chain compromise | B8 | Interim: CycloneDX SBOM generated in CI over the declared closure (`scripts/generate_sbom.py`, `security/sbom/sbom.cdx.json`) — **unsigned**; gating SCA on the lock (D-054). Target: cosign/SLSA artefact signing and installer verification [Open: D-054 → O-83]; **model artefact signing does not exist** and `MODEL_CARDS/rule-sma` is a fixture [Open: O-83, O-79] | [Open: TC-SC-002 — O-83] | Model Risk Lead |
| T-05 | Credential theft | B5 | Interim: no secret literal in the tree (`scripts/secret_scan.py`, gating in CI), `VaultRef` indirection, `secrets_mount: none` on the Analytics plane, no private signing key committed (`rtcore.signing` banned from `mcp_servers`, TC-SIG-003). Target: vault/HSM custody, workload identity and rotation [Open: H-20, O-53] (see also T-59 provider credential, T-69..T-73 signing keys) | secret scan (CI, delivered); [Open: TC-SEC-001 — H-20/O-53; gap row proposed in REVIEW_2026-09-08_security_docs_v1.1] | Security Architect |
| T-06 | Account takeover | B1 | Interim: header-asserted human identity with `mfa_enrolled`/`privileged_until` hard-coded, and the BFF refusing to start outside `RT_ENV=sim` (R-06); session and role primitives exercised. Target: IdP/OIDC session with MFA/passkeys and anomaly alerts [Open: R-06] (see also T-14) | TC-ID-003 (primitive only, not an IdP) | Backend Lead |
| T-07 | Insider misuse | B2 | Interim: maker-checker and separation-of-duties primitives; PIM window hard-coded (R-06); audit trail durable, chained and externally witnessed in dev/sim (ADR-020) with no update or delete path. Target: IdP-issued PIM window; a WORM medium and an anchor principal the audit workload cannot assume [Open: R-06, O-54] | TC-ID-004; TC-AUD-002/006/008 (no mutation path, durability, witness) | Security & Privacy Board |
| T-08 | Replay | B3, B4 | Signed intents with expiry, idempotency (see also T-31 tool-call layer, T-43 after issuer restart, T-45 authorised command) | TC-EX-003 | Integration Architect |
| T-09 | Duplicate orders / race | B4, B5 | Executor lease + fencing token (see also T-26 policy re-evaluation, T-27 ambiguous submit) | TC-EX-002/004 | Integration Architect |
| T-10 | Dependency compromise | B8 | Interim: gating SCA on the declared-closure lock (`pip-audit`, `make security-scan`, D-054) and gating SAST (`bandit` HIGH severity / MEDIUM+ confidence). Target: `--require-hashes` install, SHA-pinned actions, signed images and a signed SBOM [Open: O-83, O-84]; no image is built or signed today | CI `security-scan` (delivered); [Open: TC-SC-001 — O-83] | Cloud Architect |
| T-11 | Denial of service | B1, B7 | Interim: MCP deny bucket and per-identity quota (TC-AI-003); a per-handler deadline exists in code but **has no test** [Open: R-20]. Target: WAF/DDoS at an edge that does not exist [Open: H-05] and backpressure shedding analytics first [Open]; no per-principal rate limit exists (see also T-25) | [Open: TC-PERF-004 — R-35, H-05] | SRE Lead |
| T-12 | Audit tampering | B4, B10, all | Interim (delivered, ADR-020/D-062): audit events are rows on the `rtcore.store` seam in their **own** store file with per-row digests and a chained journal; the audit hash chain is re-verified at open and refused with `AuditIntegrityError`; **no update and no delete path exists at any layer**; sealed heads are published to a directory the audit process does not own, as anchor records forming their own chain. Target: a WORM medium and an anchor principal with its own write credential [Open: O-54]; cadence and staleness ceiling are placeholders, not decided values [Open: O-133] (see also T-16, T-74..T-79) | TC-AUD-001..009 | SRE Lead |
| T-13 | Data exfiltration | B6, B2 | Egress allowlist, canary tokens, tenant isolation (audit search/export filtered to the tenant; the export head hash is chain-wide integrity only) (see also T-83 platform-auditor read, T-88 entitlement) | TC-AI-003 (canary, quota, oversize payload), TC-AI-005 (egress denies vault and broker), TC-TEN-003 (tenant-filtered reads); v1.0 cited TC-AI-004, which is the revocation-recovery case and does not evidence egress | Security Architect |
| T-14 | Client-asserted human identity, role, MFA and PIM in the BFF (`X-Actor-*` headers; hard-coded `mfa_enrolled`/`privileged_until`) [Committee: REVIEW_C5 OBJ-1] | B1, B2 | Interim: non-human roles refused on the human path, start refused outside `RT_ENV=sim`. Target: OIDC/mTLS session with the PIM window from the IdP [Open: R-06] | TC-E2E-AUTH, TC-ID-005/006 (interim primitives, delivered) | Backend Lead / Security Architect |
| T-15 | Two-person / maker-checker bypass by one human presenting a second actor id and line [Committee: REVIEW_C5 OBJ-2] | B1, B2 | Interim: distinct `actor_id` and line compared — header-derived, therefore satisfiable by one human holding two header identities. Target: compare the authenticated subject plus session [Open: R-30, depends on R-06] | TC-AP-005 (interim); [Open: TC-ID-007 — R-30] | Backend Lead / Chief Risk Agent |
| T-16 | Audit tail truncation, in-process list mutation, backdated events [Committee: REVIEW_C5 OBJ-3] | B4, B10 | Sealed chain head with monotonic timestamps (delivered); truncation, stale backup and head mismatch now fail closed **against an externally published anchor** written by a different principal in dev/sim (`FileAnchorPublisher`), with S1 `audit.anchor_missing` / `audit.chain_verification_failed`; the event list sits behind the store seam with no mutation API. Residual: the deployment anchor identity [Open: O-54] (see also T-12, T-76..T-78) | TC-AUD-005, TC-AUD-007, TC-AUD-008 | SRE Lead / Security Architect |
| T-17 | Cross-tenant reads: BFF queries carry no tenant predicate; tenant is not an authenticated claim [Committee: REVIEW_C5 F-08] | B2 | Tenant predicate on every read; tenant resolved server-side from the principal binding; tenant-qualified limits (delivered, R-22). Residual: the tenant claim from an IdP session [Open: R-06] (see also T-64 forged header) | TC-RK-018 (limits); TC-TEN-001..004 | Backend Lead |
| T-18 | `ActorKind`/role spoof — in-process construction of a HUMAN actor by agent code [Committee: REVIEW_C5 F-09] | B3, B6 | Target: bind kind to credential at every control boundary [Open: R-33, depends on R-06]. No interim control beyond the role refusal of T-14 | [Open: TC-ID-008 — R-33] | MCP Security Agent / Backend Lead |
| T-19 | Agent or unauthorised actor disables a live jurisdiction flag (market access switched off) [Committee: REVIEW_C5 F-10] | B2 | `disable_flag` requires a human Compliance/Legal role (delivered, R-33) | TC-CP-007 | Compliance Agent / Backend Lead |
| T-20 | NetworkPolicy allow-union grants MCP pods the namespace-wide analytics egress [Committee: REVIEW_C3 F-19, REVIEW_C5 F-11] | B4, B6 | MCP pods excluded from the broad rule; effective-union checker (delivered, R-18) | TC-NET-003; cluster conformance [Open: H-05, O-42] | Security Architect / Cloud Architect |
| T-21 | Execution-plane egress to the whole internet; DNS tunnelling; edge reads the execution plane [Committee: REVIEW_C5 F-12] | B5, B6 | Per-adapter broker CIDRs (placeholder values delivered; the checker refuses 0.0.0.0/0); DNS monitoring [Open: R-32] | TC-NET-003 (checker, delivered); [Open: TC-NET-006 — R-32] | Cloud Architect |
| T-22 | Stored XSS / log injection via actor id and free-text reason [Committee: REVIEW_C5 F-05] | B1 | Output encoding in the console (delivered); CSP and control-character stripping [Open: R-34] | [Open: TC-SEC-003 — R-34] | Frontend Lead / Backend Lead |
| T-23 | Legal-hold scope miss causing fail-open deletion; unauthorised hold release [Committee: REVIEW_C5 F-18] | B2 | Enclosing-scope hold match; no schedule → suppressed; human Legal/Compliance release (delivered, O-09, D-025) | TC-CP-006; hold-release abuse case [Open: TC-CP-013 — id allocated in v1.1, test does not exist; O-09]. TC-CP-008 is the legal-record test of T-52/T-53 and is not this case | Privacy Lead / Legal Agent |
| T-24 | Redaction misses account/customer ids, IBAN, IP, payloads, reasons [Committee: REVIEW_C5 F-19] | B2 | Interim: id/IBAN/IP patterns (delivered). Target: structured field-level redaction of payloads and free-text reasons [Open: R-35] | TC-OB-002 (patterns only; not extended to structured fields) | SRE Lead / Privacy Lead |
| T-25 | Intent-flood audit amplification without a rate limit [Committee: REVIEW_C5 F-06] | B1 | MCP path: deny bucket plus quota (delivered). Target: per-principal rate limit on the human path and no audit of pre-validation floods [Open: R-35] (see also T-11) | TC-AI-003 (MCP quota); [Open: TC-PERF-004 — R-35] | SRE Lead |
| T-26 | Second live order for one intent via policy-version re-evaluation or Control-plane replay [Committee: REVIEW_C2 OBJ-1] | B4 | Gateway per-intent live-order invariant with an S1 alert; reconciliation groups live orders by intent (R-02, ADR-014, D-015) (see also T-09, T-27) | TC-EX-007, TC-RC-003 | Integration Architect / Backend Lead |
| T-27 | Ambiguous submit failure: resubmit to a non-deduping broker duplicates; the orphan is never cancelled [Committee: REVIEW_C2 OBJ-2] | B5 | Broker query before resubmit; local cancel of never-seen orders; STATUS breaks (R-09, ADR-014, D-016) (see also T-09, T-26) | TC-EX-008, TC-RC-005 | Backend Lead / Broker-Connector Lead |
| T-28 | Agent-callable `run_simulation` reaches live monitoring/execution state (global guard, alert hook, deny evidence) [Committee: REVIEW_C3 OBJ-1] | B6, B3 | Private per-platform `PlaneGuard`; template/strategy/version validation (ADR-013, R-16, D-027); worker isolation [Open] (see also T-02) | TC-AI-006 | MCP Security Agent / Backend Lead |
| T-29 | Registry trust root: a forged registry signed with the public dev key accepted; signature issued before approval [Committee: REVIEW_C3 OBJ-2] | B8, B6 | Delivered (ADR-019/D-060): asymmetric verification against a public `TrustSet`, algorithm allowlist before any cryptographic operation, purpose and `key_id` bound into the signed bytes, dev HMAC key accepted only in `RT_ENV` dev/sim, envelope-signed metadata, fixture flag, CI asserting the production refusal. Residual: the committed registry is still dev-HMAC signed under `dev-key-v0` until a ceremony key exists [Open: O-127, H-20] | TC-AI-005, TC-AI-010, TC-AI-011, TC-SIG-001..004 | MCP Security Agent / Security Architect |
| T-30 | Tenant-wide revocation as denial of service by a low-privilege agent; alert storm; silent no-op auto-actions [Committee: REVIEW_C3 OBJ-3] | B3, B6 | Scoped grant revocation; denials counted against quota; `alert.autoaction_failed` S1 (R-17, D-018) (see also T-67) | TC-AI-003, TC-OB-005 | Backend Lead / SRE Lead |
| T-31 | Replay of a signed tool call within the token TTL (tool-call layer of T-08) [Committee: REVIEW_C3 F-01] | B6, B3 | Nonce plus issued_at in the signature; per-token nonce cache; the queue refuses a replayed `intent_id` (R-15, D-019) (see also T-08, T-43, T-45, T-80) | TC-AI-009, TC-RK-015 | Backend Lead / Integration Architect |
| T-32 | Kill Switch engagement failure: a hook exception leaves the switch disengaged and unrecorded [Committee: REVIEW_C4 Objection 1] | B4 | Persist-first activation; per-hook failure capture; `killswitch.hook_failed` S1 (R-23, D-020) | TC-KS-007 | Backend Lead / SRE Lead |
| T-33 | Fail-open defaults after the risk decision (missing snapshot → LIVE, unknown mode → LIVE, stale snapshot, tenant mismatch) [Committee: REVIEW_C4 Objection 2] | B2, B4 | Explicit mode→target map; re-decision at approval; snapshot age, tenant and NAV checks (R-24) | TC-RK-018, TC-RK-019 | Backend Lead / Chief Risk Agent |
| T-34 | Exposure-cap evasion by splitting orders across instruments; trapped over-limit book [Committee: REVIEW_C4 Objection 3] | B3 | Open orders projected into exposure; risk-reducing exemption (R-25, D-022; TRC decision O-46). This row covers the **exposure** cap only; the **position** cap is T-40, and neither row closes the other | TC-RK-017 | Backend Lead / Trading Risk Committee |
| T-35 | Hung MCP handler blocks the BFF event loop including `/v1/killswitch` [Committee: REVIEW_C3 F-05] | B1, B6 | Delivered in code: a pre-emptive handler deadline (`future.result(timeout=spec.timeout_s)` → `TIMEOUT` with `mcp.handler_timeout`) and the BFF calling the runtime off the event loop (R-20) — **no test covers the timeout**; TC-AI-010 covers handler *errors* only. Target: a Kill Switch listener isolated from the MCP handlers [Open: R-20 residual] | TC-AI-010 (handler errors only, delivered); timeout case [Open: R-20]; [Open: TC-SEC-004 — id allocated in v1.1 for listener isolation, test does not exist; R-20]; supersedes the invalid id "TC-SEC-MCP" of v1.0 | Backend Lead / SRE Lead |
| T-36 | Halt/restore cycle used as an ungated promotion; agent sets trading status or halts an account [Committee: REVIEW_C4 F-02, F-03] | B2 | Restore ceiling = enabled feature, never autonomy; human-only status/halt (R-28, D-021) | TC-KS-004; agent abuse case [Open: R-28] | Backend Lead |
| T-37 | Liquidation executed under an unapproved `liquidation_policy_ref` [Committee: REVIEW_C4 F-01] | B4 | Approved-policy registry gate with CANCEL_ONLY fallback (delivered as `KillSwitchService.approved_liquidation_policies`, R-27); maker-checker on the emergency-policy fields [Open: O-08] | fallback exercised by an unindexed unit test; [Open: TC-KS-011 — id allocated in v1.1, no TC row exists; R-27, O-08] | Backend Lead / Trading Risk Committee |
| T-38 | Authorised command delivered (bus/outbox path) after a Kill Switch or halt engaged; the gateway executes it [Committee: GATE_C_2026-09-07 V-C1] | B3/B4 | Gateway permission oracle at submit and retry (`ExecutionBlocked`; R-36, D-028) | TC-EX-009 (negative, recovery) | Backend Lead / Chief Risk Agent |
| T-39 | Forged or altered `OrderCommand` from a caller inside the Control plane (or replayed from the bus with edits); the authorisation key itself leaking through the process environment or logs [Committee: GATE_C_2026-09-07 V-C2; COUNCIL_2026-09-08_gate_B_spb_docs M-9] | B3 | Control-plane authorisation MAC verified by the gateway; decision provenance check (`CommandNotAuthorised`; R-37, D-029). Delivered since ADR-019/D-060: the gateway may hold a `PublicKeyCommandVerifier` with no signing capability and no private bytes reachable by attribute walk; the dev/sim default remains the in-process HMAC. The authorisation key is supplied to the composition root at start-up (`authorisation_key`) — never logged, never in an agent context; vault/KMS supply and rotation [Open: O-53, H-20] | TC-EX-009 (positive, abuse), TC-SIG-001..004 | Backend Lead / Security Architect |
| T-40 | Position cap evasion by splitting into a resting order plus a new order [Committee: GATE_C_2026-09-07 IVA-03] | B2 | Committed position = positions + open orders in `chk_caps` (R-38). This row covers the **position** cap only; the exposure cap is T-34 | TC-RK-021 | Backend Lead |
| T-41 | Strategy owner approves an intent of their own strategy [Committee: GATE_C_2026-09-07 IVA-04] | B2 | The pipeline passes `strategy_owner_id` from the registry to the approval queue (R-39) | TC-AP-005 | Backend Lead |
| T-42 | Fail-open corners: agent rejects a controlled change; unlabelled environment treated as sim; published dev key accepted via an environment variable [Committee: GATE_B_2026-09-07 IVA-05/06/07] | B8 | Human-only `reject()`; `RT_ENV == "sim"` exact match; dev key black-listed outside dev/sim (R-40) | TC-ID-005, TC-E2E-AUTH, TC-AI-011, TC-PKG-002 | Backend Lead / Security Architect |
| T-43 | Replay of a captured signed tool call after an issuer restart [Committee: GATE_B_2026-09-07 IVA-08] | B6, B3 | Per-agent nonce journal persisted across restarts as append-only JSONL (D-031). Target: the journal on the `Store` seam with digests and chain [Open: O-55, O-118] (see also T-80) | TC-AI-011 | Backend Lead |
| T-44 | Order live at the broker under an active Kill Switch after a failed cancel hook, re-adopted silently on retry [Committee: REVALIDATION IVA-19] | B3/B4 | Cancel-on-adoption when blocked; S1 `execution.live_under_block` → Kill Switch account (R-41, D-032) | TC-EX-010 (recovery) | Backend Lead / Chief Risk Agent |
| T-45 | Replay of a stale or already-executed authorised command after a gateway restart or after the Kill Switch is lifted [Committee: REVALIDATION IVA-21] | B3 | Five-minute authorisation age, one-shot MAC, one order per decision, intent-state check (R-42, D-033); key validity checked at `authorised_at` so clock skew cannot resurrect a retired key (ADR-019) (see also T-08, T-31, T-72) | TC-EX-010 (abuse), TC-DUR-001, TC-EX-011, TC-SIG-002 | Backend Lead |
| T-46 | Forged command used as a cross-account halting primitive through the auto Kill Switch; inbox probing with edited commands; crash on unknown account [Committee: REVALIDATION IVA-23..25] | B3/B4 | Auto-action none on unauthorised commands; MAC before inbox; inputs fail closed (R-45, D-034) | TC-EX-010 (negative) | Backend Lead / Security Architect |
| T-47 | An MCP host (or any local process able to spawn `rt365 mcp-serve`) reaches the six tools; a client tries to assert another identity or call a forbidden tool through the transport [Committee: ADR-016] | B6 | Identity bound by the host process (client `_meta` ignored); registry-gated tool list; every call through `ToolRuntime.call`; 1 MiB frame cap; sim only (R-47) | TC-AI-012..015 | Backend Lead / MCP Security Agent |
| T-48 | A delivery agent authors an artefact it reviews or approves, or edits the guard that constrains it [Committee: ADR-016; roster 1e8555e; COUNCIL_2026-09-08_gate_B_spb_docs §3.7 and M-8] | B8 | Generated roster plus a PreToolUse hook on **Edit/Write/MultiEdit/NotebookEdit only**, using the harness-supplied `agent_type` (PROBE_O58). **Bash writes are uninspected (R-46) and are the default path under harness auto mode (R-54); the guard modules (`scripts/agent_guard.py`, `apps/cli/rt365_cli/agent_guard.py`) and the protected service paths are inside every builder's roster scope, so the guard is editable by its own subjects [Open: roster narrowing, O-131].** The guard is a delivery guard rail, never a control of record. Controls of record: CODEOWNERS with placeholder team handles [Open: O-20], branch protection [Open: H-01], CI path-ownership/authorship check [Open: O-85, O-131], IVA authorship review. Nothing in a gate record may cite TC-AGT as proof that segregation is enforced | TC-AGT-001..004 (roster consistency and the Edit/Write path only) | Product Owner / MCP Security Agent |
| T-49 | A tampered or incomplete resource bundle in an installed or frozen build substitutes policies, or the build silently falls back to another root [Committee: ADR-016] | B8 | Marker check per candidate root, explicit `RT365_HOME` never falls back, signed-registry verification at load | TC-PKG-003, TC-PKG-004 | Security Architect |
| T-50 | A distributed binary is run without an environment label or with a non-sim label against the fixture registry [Committee: ADR-016] | B8 | The CLI refuses unlabelled environments; only sim is servable; the fixture registry is refused outside dev/sim; the release workflow asserts both | TC-PKG-002, TC-PKG-005; release workflow smoke test | SRE Lead |
| T-51 | Jurisdiction enabled by two hands of the same function (Compliance + Compliance), by the Product Owner, or by an agent [Committee: COUNCIL_2026-09-08_gate_A_compliance_legal F-1] | B2 | Legal record signed only by a human Legal Agent; flag activated only by a human Compliance role; signer != activator; the Product Owner and agents are never dual-key hands (R-49) | TC-CP-007 | Backend Lead / Compliance Agent |
| T-52 | Fabricated legal record: a string that looks like a reference satisfies the legal half of the dual key (council F-2/P-2) | B2 | `record_legal` accepts only `LegalRecordRef` (id pattern, named signing entity, date, 64-hex SHA-256); dict/str refused with ControlDenied; the record is anchored in `jurisdiction.legal.recorded` with a correlation_id; only the stored cell is updated | TC-CP-008 | Backend Lead / Compliance Agent |
| T-53 | Sim evidence dressed as real: a real-looking record on the ZZ cell, a SIM- record on a real-country cell, or a forged `simulated=False` label (council X-6, D-012) | B2 | The schema validator derives the label from the country code and refuses SIM-/real mismatches in both directions; the registry re-validates on record | TC-CP-008, TC-GLO-002/003 | Backend Lead |
| T-54 | Self-declared classification bypasses retail protections through an eligibility input (council X-5/P-4, D-045) | B3 | CP-CLASS for absent, self-declared, self-assessed or future-dated evidence; evidence is a typed model (no free strings, no unknown basis) | TC-CP-010, TC-CP-011 | Backend Lead / Compliance Agent |
| T-55 | Mode operated without recorded customer consent or without acknowledged disclosures (Q-J06, Q-P04) | B3 | CP-MODE-CONSENT for SUPERVISED/BOUNDED without a non-future consent for that exact mode; CP-DISCL for a missing, stale or future acknowledgement against the cell's required version; withdrawal takes effect on the next decision | TC-CP-010, TC-CP-012 | Backend Lead |
| T-56 | Persona read as entitlement: a control/read/governance role (tenant admin in particular) changes a mode, restores from halt or submits an intent (D-045) | B1/B3 | Registry MODE_CHANGERS, RBAC permissions and BFF 403 evidenced for 17 roles; the agent path is re-asserted | TC-ID-006 | Backend Lead / Security Architect |
| T-57 | Cell granted for one CustomerType x mode read as covering another | B2 | Six-dimension exact match evidenced for all 4 x 3 pairs against a live PROFESSIONAL/PAPER cell | TC-CP-009 | Backend Lead / Compliance Agent |
| T-58 | Durable market-data store bypass: a writer reaching the store outside the ingest path forges knowledge time or provenance (ARB C-13-2) | B7/B2 | Target: an adapter seam with append-only writes stamped by the ingest service and a knowledge-time monotonicity check on read — **designed, not built** [Open: D-056, O-13] | [Open: TC-MD-005..008 — D-056] | Backend Lead / Data Architect |
| T-59 | Provider boundary: a model provider receives a secret, a broker route, a limit write path or unmasked customer data through the model gateway (D-052) | B9 | Target (design decided, **not built**): a single gateway service in the Analytics plane holding the only provider credential; masking and provenance labels at the boundary; processing region pinned to the tenant `residency_region`; egress allowlist in code and in NetworkPolicy; fail closed to Observe on outage [Open: O-79, D-052]. No provider is wired, no terms are signed and no provider capability is asserted [Open: H-06, Q-05-1..14] (see also T-05) | [Open: TC-AI-016..019 — O-79] | Security Architect / MCP Security Agent |
| T-60 | A process restart (crash, deploy, failover) forgets a consumed grant, a fencing token, a live order for an intent or an engaged Kill Switch, so a replayed or late command executes, a lower token is reissued, or the switch is silently disengaged [Committee: ADR-018; R-05, R-23] | B4, B5 | Every such invariant is a store row; the fencing counter is a store sequence; the gateway reads through the store; the risk engine and the oracle read activations from the store. Residual: the control-plane intent tracker and the composition root's decision index are still in memory, so pre-restart intents are **refused, not resumed** (fail closed) [Open: O-33, O-113] | TC-DUR-001, TC-EX-011, TC-KS-010 | Backend Lead / Trading Domain Lead |
| T-61 | An actor with write access to the store file edits, deletes or re-journals rows (for example flips an activation to inactive or removes a consumed grant) [Committee: ADR-018] | B10 (host), B4 | Per-row digest checked on every read; hash-chained journal replayed and compared with the state at open; any mismatch refuses the store (`StoreIntegrityError`). Digests are **unkeyed**: tamper-evident against inconsistent edits, not tamper-proof — a consistent rewrite of rows plus journal is undetectable by construction. Chosen remedy (D-061 (5), O-128): anchor the journal head into the audit chain whose head is anchored off-box by a different principal (O-54); the audit half is delivered (ADR-020/D-062) and **the journal-head anchoring itself is not implemented** [Open: O-110, O-128]; OS-level file hygiene is mandatory alongside and is not evidential [Open: O-128] | TC-DUR-003 (edit, delete, journal drop; **the consistent rewrite is not attempted** — RT-05 [Open: RED_TEAM_PLAN, H-10]) | Security Architect |
| T-62 | The store is unavailable or corrupt at submit, retry or cancel; a gateway that guesses proceeds without its indexes [Committee: ADR-018] | B4, B10 (host) | `StoreError` propagates before any adapter call; S1 `execution.store_unavailable` catalogued (O-112); the gateway never submits without a readable store (see also T-85 for lock starvation) | TC-DUR-003 | Backend Lead / SRE Lead |
| T-63 | Two executors on one store both believe they hold the lease (split brain) [Source: 03; ADR-002] | B4, B10 (host) | acquire/preempt inside BEGIN IMMEDIATE; `is_valid` reads the shared row; a preemption by one is stale for the other in its own process; cross-OS-process and cross-host behaviour is not verified [Open: O-117] | TC-DUR-002 | Trading Domain Lead |
| T-64 | Forged tenant claim at the edge: an attacker sets `X-Actor-Tenant` on a header-authenticated request [Committee: BUILD_E01] | B2 | Tenant resolved server-side from the principal binding; the header is refused (403) and audited `tenant.scope.denied` reason `TENANT_HEADER_FORGED` with a correlation id (see also T-17) | TC-TEN-003 | Backend Lead |
| T-65 | Cross-tenant identity mint: a compromised identity-service caller asks for tenant A over account B [Committee: BUILD_E01] | B2, B6 | `IdentityIssuer(scope_valid)` refuses at mint and the call-time SCOPE check remains; audited `mcp.identity.refused` | TC-TEN-002 | Backend Lead |
| T-66 | Queue draining across tenants: `/process` used to pop and discard other tenants' intents while searching [Committee: BUILD_E01] | B2 | `IntentQueue.take(intent_id, tenant_id)` removes only the caller's tenant's intent; queue depth unchanged after a foreign attempt | TC-TEN-003 | Backend Lead |
| T-67 | Collateral grant revocation during a tenant suspension: denials used to trigger the non-allowlisted auto-action, revoking every scope's grant so a restore did not restore [Committee: BUILD_E01] | B6 | `TENANT_REVOKED` carries no auto-action; alert `mcp.tenant_revoked` (S2, page); two-person restore (see also T-30) | TC-TEN-004 | MCP Security Agent |
| T-68 | Cross-tenant blast radius of ASSET/VENUE/STRATEGY halts engaged by a tenant-bound principal (denial of service between tenants) [Committee: BUILD_E01 C-3] | B2 | **Accepted risk R-57** — halting is fail-safe for capital and unsafe for availability; the Trading Risk Committee is asked to confirm the acceptance, which is not accepted by its author. Compensating controls: deactivation of platform-wide levels refused for tenant principals (delivered); narrowing by a platform operator [Open: O-120]; alert `killswitch.cross_tenant_scope` (S2) on a cross-tenant scope activation [Open: O-132 — proposed, not catalogued, does not exist] | TC-TEN-003 (deactivation refusal only); blast-radius and platform-operator cases [Open: O-120, O-132] | Chief Risk Agent |
| T-69 | A holder of the verification key forges a signed tool registry or a command grant: the symmetric key was held by every verifier, including installers and operator machines [Committee: ADR-019; O-22, R-44] | B4, B6 | Asymmetric signatures; verifiers hold public keys only (`rtcore.trust`); `rtcore.signing` banned from `mcp_servers`. Residual: dev/sim still runs HMAC by default and the committed registry is dev-signed [Open: O-127]; no production key pair exists [Open: H-20] | TC-SIG-001, TC-SIG-003, TC-AI-005 | Security Architect |
| T-70 | Algorithm confusion: an HMAC envelope claims Ed25519, an Ed25519 envelope claims HMAC, an unknown algorithm is offered, or the public key is presented as an HMAC key [Committee: ADR-019] | B4, B5 | Allowlist checked before any cryptographic operation; HMAC refused outside dev/sim from any source | TC-SIG-003 | MCP Security Agent |
| T-71 | Cross-purpose replay: a registry signature presented as a command grant, or a signature by key A replayed under key_id B [Committee: ADR-019] | B5, B6 | `signed_message(purpose, key_id, message)` binds purpose and key_id into the signed bytes | TC-SIG-003 | Security Architect |
| T-72 | A compromised or retired signing key keeps verifying because rotation is only a timestamp [Committee: ADR-019] | B4 | `TrustSet.retire()` sets revoked and refuses absolutely whatever the timestamps say; a key_id is never reused; overlap is explicit; validity is checked at `authorised_at` so clock skew cannot resurrect a key. Residual: the rotation *ceremony* is a human act that has never run, and the interval and overlap in D-053 are proposals, not decided values [Open: H-20, O-126] | TC-SIG-002, TC-SIG-004 | Security Architect |
| T-73 | A signing handle is reachable from a component that must not sign (gateway verifier, MCP runtime, registry) [Committee: ADR-019; supersedes the wrapper mitigation of R-44] | B5, B6 | `PublicKeyCommandVerifier` holds only a trust set: no sign, no seed, nothing in repr; asserted by walking the object graph. Residual: in-process Python has no isolation boundary; the deployment boundary is a separate gateway process holding verification material only [Open: O-53] | TC-SIG-003 | Backend Lead |
| T-74 | The audit trail is lost or partly lost on a restart while control state survives, so an auditor cannot reconstruct what happened to durable state [Committee: ADR-020; D-058, D-062] | B4, B10 (host) | Audit events are durable rows on the store seam in **their own store file** (`audit_state.sqlite`, never the control store's); the sequence is allocated by the store; chain and seals are read back and verified at open (see also T-86) | TC-AUD-006 | Backend Lead |
| T-75 | An actor rewrites audit rows and recomputes every unkeyed seam digest (the offline attacker O-110 admits) [Committee: ADR-020] | B10 (host), B4 | The audit hash chain is verified at open independently of the seam digests; the store is refused with `AuditIntegrityError` | TC-AUD-008 | Security Architect |
| T-76 | The audit tail is truncated, or a stale backup is restored, and the store verifies against itself [Committee: REVIEW_C5 OBJ-3, IVA-09] | B4 | The externally published anchor is compared with the chain: a shorter chain, a head mismatch, or a seal the witness does not carry all fail closed. Residual: in dev/sim the witness is a directory on the same host (`FileAnchorPublisher`); a WORM medium and a principal the audit workload cannot assume are a deployment act [Open: O-54]; the staleness ceiling is a placeholder, not a decided value [Open: O-133] | TC-AUD-007 | SRE Lead |
| T-77 | The external witness is edited, shortened, removed from the middle, or lost [Committee: ADR-020] | B4, B10 (host) | Anchor records form their own hash chain, verified before use; a missing or broken witness fails closed and alerts (`audit.anchor_missing`, S1, catalogued); there is no automatic re-anchoring. Recovery is restore-then-verify-then-seal, never disabling the check; the runbook does not yet say so [Open: O-134] | TC-AUD-007, TC-AUD-008, TC-AUD-009 | SRE Lead |
| T-78 | An anchor is published over a tampered or rolled-back chain, laundering it [Committee: ADR-020] | B4 | A publication whose head contradicts the last published anchor is refused, whoever asks; a scheduled publication may never create the first anchor of a non-empty chain — only an operator's explicit and audited seal may re-anchor | TC-AUD-007, TC-AUD-008 | Backend Lead |
| T-79 | The anchor publisher principal, or anything holding its handle, reaches an audit write or delete path [Committee: ADR-020] | B4, B6 | The publisher receives only a sealed `ChainHead`; `AnchorHandle` exposes exactly head and length; no audit mutation API exists at any layer to reach | TC-AUD-008 | Security Architect |
| T-80 | A revocation or nonce journal line is deleted or rewritten: a revoked tool, identity or tenant grant silently returns, or a consumed nonce becomes replayable [Committee: COUNCIL_2026-09-08_gate_B_spb_docs M-1] | B10 (host), B6 | Interim: append-only JSONL reloaded at start (`mcp_servers/revocation.py`, `identity.py`) with record validation only — **no digest, no chain; a deleted line is a silent un-revocation**. Target (D-061 (6)): both journals on the `Store` seam inside the Control-plane store, inheriting per-row digests, the chained journal and the O-128 anchor [Open: O-55, O-118, O-128] (see also T-43, T-67) | [Open — quartet owed with O-55; no id allocated] | MCP Security Agent / Backend Lead |
| T-81 | The control store is restored from a backup without the matching revocation and nonce journals, or the reverse: consumed grants and revoked identities re-open — a restore is a replay [Committee: D-058 (e); COUNCIL_2026-09-08_gate_B_spb_docs M-2] | B10 (host), B4 | Interim: with `store_dir` both journals default to the control store's directory, so one directory is one restore unit (dev/sim only, D-058 e). Target: control tables and journals named as one restore unit in DR_PLAN, and `verify()` against the last anchor refusing a stale restore [Open: O-118, O-128, H-19] | [Open — recovery case of the O-128 quartet] | SRE Lead / Backend Lead |
| T-82 | A platform-wide human action (PLATFORM/TENANT halt, platform limit, tenant suspension) is attempted through a header-style or CLI identity instead of an IdP-bound platform-operator claim [Committee: BUILD_E01 C-2; D-059, D-061 (7)] | B1, B2 | Delivered: the tenant BFF refuses platform levels for every tenant-bound principal (403, fail closed) — there is therefore **no human platform path at all today** (R-56). Target: a tenant-less, time-bound IdP claim on a separate route family, two-person deactivation, every action audited with tenant `-`; **no header- or CLI-based platform identity may be added as a stop-gap** [Open: O-120, R-06] | TC-TEN-003 (refusal); platform-operator quartet [Open: O-120] | Backend Lead / Security Architect |
| T-83 | A platform-auditor unfiltered read is used to exfiltrate another tenant's rows or to move personal data across a residency boundary [Committee: BUILD_E01 C-7; D-059, D-061 (7)] | B2 | Delivered: no unfiltered read path exists; audit reads are tenant-filtered and platform rows carry tenant `-`. Target envelope, set by the Privacy Lead before any build: a read-only IdP-bound 3rd-line claim; every unfiltered read is itself an audited event carrying the reason and the tenant set touched; payloads pseudonymised by default and revealed only under an investigation or legal-hold reference; residency answered by the DPIA — **no assertion is made about any jurisdiction's law** [Open: O-121, O-10, R-35] (see also T-13, T-24) | [Open: O-121] | Backend Lead / Privacy Lead |
| T-84 | A pending first-person Kill Switch deactivation persists across restarts indefinitely; a second hand completes it long after the context changed [Committee: BUILD_E07 C-9] | B2, B4 | Interim: the two-person rule and the audit trail stand; **no TTL exists**. Target: a pending-deactivation TTL with the expiry audited, and a post-incident-review reference for PLATFORM/TENANT deactivation; the TTL value is a risk decision, not a number this document sets [Open: R-23] | [Open: R-23] | Backend Lead / Chief Risk Agent |
| T-85 | A stuck or hostile writer holds the store lock, so every gateway submit, retry and cancel fails closed (`busy_timeout` then `StoreError`): the Execution plane cannot flatten a position [Committee: BUILD_E07 C-7/C-13] | B10 (host), B4 | Delivered: S1 `execution.store_unavailable` (catalogued, page, runbook). Target: lock-holder identification, a cancel-path priority, and the Execution-plane store physically separate from shadow [Open: O-117, O-115]. The `busy_timeout` in the code is an implementation placeholder, not a decided threshold [Open: O-117] (see also T-62) | TC-DUR-003 (fail closed); multi-process and chaos cases [Open: O-117] | SRE Lead / Performance & Chaos Lead |
| T-86 | Control state survives a restart while its audit trail does not: an activation or a consumed grant without the events that produced it [Committee: BUILD_E07 C-10; register B-5] | DF-07, B4 | Remediated in dev/sim since ADR-020/D-062: the audit trail is durable on the seam in its own store, chained, verified at open and witnessed by an external anchor chain; control-store journal rows carry a `correlation_id` but are not the audit. Residual: the anchor principal is a directory on the same host in dev/sim [Open: O-54]; retention, legal hold and erasure against an append-only chain are unanswered [Open: O-135] (see also T-74) | TC-AUD-006..009 | SRE Lead |
| T-87 | A forged, replayed or unauthorised regulatory report or submission passes through a regulatory adapter [Source: 06; DATA_FLOWS DF-09] | B11 | Target: a per-jurisdiction adapter enabled only by the dual key (human Legal record plus human Compliance activation — delivered for the flag itself, T-51); the report signed by a human Compliance role; idempotent submission with a replay refusal. **No adapter, no report path and no test exist**; no regulatory obligation, format, recipient or deadline is asserted [Open: O-10, COMPLIANCE_MATRIX] | [Open — no TC id allocated; the adapter does not exist] | Compliance Agent / Integration Architect |
| T-88 | A licensed or unlicensed market-data field leaves the entitlement boundary into an AI context, an export or a second tenant [Source: DATA_FLOWS DF-01/DF-02] | B7, B3 | Target: entitlement check at ingest and masking of unlicensed fields at DF-02 — **documented in DATA_FLOWS, not implemented and not tested**; no data licence is held and no provider entitlement is asserted [Open: H-06, O-129 DATA_FLOWS v1.1] (see also T-03, T-13) | [Open — entitlement case owed with TC-MD-005..008, D-056] | Data Architect / Data Engineering Lead |

## Coverage and honesty statement [Committee]
- Rows whose control quartet exists in `docs/TEST_CASES/` and passes at this commit: every row whose Test ID column carries no `[Open]` marker.
- Rows carrying an `[Open]` test id or an `[Open]` clause in the test column: T-04, T-05, T-11, T-15, T-18, T-20, T-21, T-22, T-23, T-25, T-35, T-36, T-37, T-58, T-59, T-61, T-68, T-80..T-88. The test named there **does not exist**. **No statement of the form "the threat model is covered by tests" may appear in any gate report** [Committee: COUNCIL_2026-09-08_gate_B_spb_docs SPB-PO-10].
- Ids allocated in v1.1 for tests that do not exist: TC-CP-013 (T-23 hold release), TC-SEC-004 (T-35 listener isolation), TC-KS-011 (T-37 liquidation fallback). Allocation is bookkeeping, never evidence.
- Known cross-reference defect not repairable from this document: `docs/ADRs/ADR-018.md` §Threat-model delta cites "T-52, T-53, T-54" for what are in fact **T-60, T-61, T-62** (T-52..T-54 are the legal-record rows). Correction owed by the Enterprise Architect / build-e07 [Open: RAID row proposed in `docs/SESSIONS/REVIEW_2026-09-08_security_docs_v1.1.md`]. RAID types O-110 as a Risk under an O- id; aligning the type or the id is owed to the ledger owner [Open].

## Provenance footnote
Rows T-01..T-13: the original blueprint-06 baseline (Security Architect). T-14..T-37: review cycle 1 delta (2026-09-07) compiled by the Delivery Orchestrator (AI); local threat ids in the review packets map to these via `docs/RAID_LOG.md` §Review ID renumbering map. T-38..T-46: IVA remediation commits. T-47..T-50: ADR-016. T-51..T-57: Gate A compliance/legal council and E06. T-58..T-59: IVA id allocation (O-102). T-60..T-63: E07 (ADR-018). T-64..T-68: E01. T-69..T-73: E13 (ADR-019). T-74..T-79: E11 (ADR-020). T-80..T-88 and every v1.1 condition edit: authored by the Security Architect on the Board's conditions TM-C1..TM-C12.

**Author of record is the Security Architect. T-38..T-79 were transcribed into this file and committed by the Product Owner delegate session from the build and council packets named above; the same delegate decides acceptance under D-040.** Author, transcriber, reviewer, recommender and decider are distinct roles but not distinct seats for every row, which the Board raised as SPB-PO-4 and which only the human Product Owner can settle. Reviewer: Red-Team & Pen-Test Lead (signature: `<REVIEW_ packet>` — [Open], not written). Board recommendation: `docs/SESSIONS/COUNCIL_2026-09-08_gate_B_spb_docs.md` §1.7 (RECOMMEND ACCEPT WITH CONDITIONS). T-59 and the B9 boundary wording originate in the Board chair's earlier packet; the chair recused, and the Independent Validation Agent is asked to review that row with the Red-Team Lead (TM-C12). Decision: pending with the Product Owner (D-039/D-040). A test id tagged `[Open]` does not exist yet.
