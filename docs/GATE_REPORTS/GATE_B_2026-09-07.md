# Gate B — Architecture: Independent Validation Report (2026-09-07)

| Field | Value |
|---|---|
| Gate | B — Architecture [Source: 12]; passing authorises development and simulation environments only |
| Commit validated | `09a6e716d50ac9b89a26a9f295fa54509b4492c3` [Verified: `git rev-parse HEAD`] |
| Environment tag of all evidence | dev/sim only [Source: 00] |
| Author | Independent Validation Agent (AI) — 3rd line, owns no artefact under review [Source: 13] |
| Reviewer | pending |
| Approver | pending (Committee chair) |
| Verdict (recommendation only) | **ACCEPT WITH CONDITIONS** (dev/sim evidence only; entry criterion "Gate A passed" is NOT met — see §5) |

> This report is a recommendation to the Committee. No gate is passed by this report. The human approver of record is pending. Nothing here authorises promotion beyond dev/sim, implies regulatory permission, production readiness or profitability [Source: 00].

## 1. Scope and method

**Read [Verified: file reads at HEAD]:** the governing documents listed in GATE_A_2026-09-07.md §1, plus docs/THREAT_MODEL.md, docs/DATA_FLOWS.md, docs/CAPACITY_MODEL.md, docs/ADRs/ADR-001..012, docs/RACI.md, .github/CODEOWNERS, .github/workflows/ci.yml, Makefile, infra/kubernetes/network-policies/*.yaml, mcp/policies/{runtime,egress,allowlist.tenant-sim}.yaml, mcp/policies/tool_registry{,.signed}.json, security/README.md, scripts/{verify_tool_registry,check_network_policies,secret_scan}.py, and the control-bearing source: libs/core/rtcore/{planes,lines,ids,schemas/*}.py, services/risk/risk_engine/{engine,policy}.py, services/killswitch/killswitch_service/service.py, services/identity/identity_service/{makerchecker,rbac,accounts}.py, services/approval/approval_service/queue.py, services/execution/execution_gateway/{gateway,lease}.py, services/oms/oms/{pipeline,intent_queue,outbox}.py, services/audit/audit_service/store.py, mcp/servers/mcp_servers/{identity,runtime,registry,revocation}.py, apps/web/web_bff/{app,platform}.py, test/conftest.py, test/evidence_plugin.py and every quartet test file's IDs/docstrings.

**Ran [Verified]** (`PYTHONPATH` per Makefile, `RT_ENV=sim`):

| Command | Result |
|---|---|
| `timeout 1500 python3 -m pytest -q -p no:cacheprovider -W ignore` | 138 passed, exit 0 |
| `make lint` (ruff check + format --check) | All checks passed; 118 files formatted |
| `make typecheck` (mypy, 89 source files) | no issues |
| `python3 scripts/export_event_schemas.py --check` | OK: 21 event schemas match the models |
| `python3 scripts/check_network_policies.py` | OK: plane invariants (TC-NET) |
| `python3 scripts/verify_tool_registry.py` | OK: registry 0.1.0 (dev key), 6 tools; NOTE: sim FIXTURE (approvals pending) |
| `python3 scripts/verify_tool_registry.py --production` | exit 1 — "no RT_MCP_REGISTRY_KEY configured and RT_ENV='production' is not a dev/sim environment; refusing the dev key" (also exit 1 for paper, shadow, pilot labels) |
| `python3 scripts/secret_scan.py` | OK: 354 files, no findings |
| Ten probe scripts (scratchpad only) | see §3 |

Not run: `make certify-broker` and `make evidence`, because both overwrite files under docs/ (scripts/certify_broker.py line 22; scripts/evidence_report.py line 57). The certification harness was instead executed in memory (GATE_C §3, probe I).

**Re-derived myself:** every claim in §2 and §3 was checked against source or by execution, not taken from RELEASE_CHECKLIST, the weekly report, or the builder's review packets. Where a builder test exists I read its body to judge whether it covers more than the happy path.

**Independence note:** see GATE_A §1 (working tree modified by another process during validation; those uncommitted changes are excluded).

## 2. Criterion-by-criterion

Exit evidence [Source: 12]: threat model, data flows, ADRs, capacity model, control ownership. Approving bodies: ARB, Security & Privacy Board. IVA veto minimum: any critical control without a named 2nd-line owner.

| Criterion | Evidence I verified | Verdict | Notes |
|---|---|---|---|
| Entry: Gate A passed | GATE_A_2026-09-07.md verdict REJECT; no DECISION_LOG entry records a Gate A pass [Verified] | NOT MET | By procedure the gate cannot be convened. This report evaluates the exit evidence so the Committee can see the state of the architecture; see §5. |
| Threat model | THREAT_MODEL.md: 8 trust boundaries, 13 threats T-01..T-13 each with control, test ID and owner; status "Draft v1.0"; AUDIT_EVIDENCE_INDEX #2 "board minutes none" [Verified]. Test IDs cited by T-04 (TC-SC-002), T-05 (TC-SEC-001), T-06 partly, T-10 (TC-SC-001), T-11 (TC-PERF-004), T-13 (TC-TEN-003) **do not exist** anywhere in test/ or the evidence report [Verified: `grep -rl` → 0 files]. T-01/02/03/08/09/12/13(TC-AI-004) cite tests that exist and pass. | PARTIAL | Five of thirteen threat rows are documents only (control named, no test). Not approved by the Security & Privacy Board. Threat deltas exist in SESSIONS/REVIEW_C5 [Committee]. My probes add threat-model deltas (see IVA-01, IVA-02, IVA-06, IVA-07 in §4). |
| Data flows | DATA_FLOWS.md DF-01..DF-09 with classification and controls; DF-04 "signed tool call" is implemented (probe F); DF-05 "idempotency, fencing token" implemented (TC-EX-002/003 read; gateway.py lines 118-148); DF-06 vault credentials: `VaultRef` only, raw credential refused (sim-broker cert TC-BR-001b) [Verified] | MET (dev/sim) | DF-05 says nothing about authenticating the command's origin; see IVA-02. |
| ADRs | ADR-001..012 present; **all twelve are "Status: Proposed"**; DECISION_LOG D-004..D-009 "pending ARB / pending S&P Board / pending Product Council" [Verified: `head -6 docs/ADRs/*.md`] | PARTIAL | The decisions are documented with ≥2 alternatives (spot-checked ADR-009, 010, 011). None is accepted by the ARB. |
| Capacity model | CAPACITY_MODEL.md "Skeleton v0.1 — numbers [Open: O-03, O-13, O-17]"; states "No number in this document is set" [Verified] | PARTIAL | Structure (scale units, shed policy, SLI plan) exists; RAID marks O-13 and O-17 as needed by Gate B and open. |
| Control ownership (veto criterion) | RACI.md names a 2nd-line accountable role for: risk limits (Chief Risk Agent/TRC), strategy approval (Model Risk Committee), jurisdiction (C&L Committee), MCP tool registration (MCP Security Agent), security/privacy (S&P Board), Kill Switch deactivation (TRC). CODEOWNERS protects /services/risk, /compliance, /execution, /killswitch, /mcp/policies, /security, /contracts, network policies and four docs with 2nd-line/3rd-line team handles, "placeholders until … [Open: O-20]" [Verified]. People not appointed (H-01); deputies not named (O-19). | PARTIAL — no veto | Every critical control has a named 2nd-line *role*. No control has a named 2nd-line *person*, and branch protection is not enabled (O-20). I do not treat role-level naming as the veto ground, but the Committee should decide whether "named owner" means a person. |
| Plane topology enforced (ADR-001, NFR-SEC-01) | check_network_policies.py passes 7 invariants over 5 manifests (default-deny in analytics/control/execution; no analytics→execution/vault; only execution holds an ipBlock; MCP pods excluded from the namespace-wide analytics egress) [Verified: script run; manifests read]. In-process PlaneGuard denies analytics→execution and unattributed callers (probe B4). | MET (dev/sim) — manifests only | Manifests have never been applied to a cluster (H-05 open; infra/iac/README only). The in-process guard is a convention: any in-process caller may `enter(Plane.CONTROL)` (probe B4, IVA-11). |
| MCP forbidden capabilities structurally impossible (FR-07/FR-09) | Registry schema enum allows exactly the six tools; write-class only `submit_trade_intent` with queue-only scope; runtime.yaml filesystem read-only / shell absent / secrets none; egress allowlist excludes vault/broker; `mcp_servers` package references no execution/broker/killswitch/limit-write module (probe B5); calls to non-existent `change_limit`, `activate_kill_switch`, `set_mode`, `delete_audit` → TOOL_NOT_REGISTERED and audited (probe J1) [Verified] | MET (dev/sim) | Registry signing is HMAC with the documented dev key and a fixture flag; see IVA-07 and O-22. |
| Signed tool registry (AEI #10) | Signature over an envelope (registry, key_id, algorithm, environment_tag, version, signed_at, fixture); tampering one field → refused (probe G6); RT_ENV unset → dev key refused (G5); production label → refused (G1) [Verified] | MET (dev/sim) with a defect | IVA-07: the dev key is not black-listed — supplied via `RT_MCP_REGISTRY_KEY` it signs a production-tagged non-fixture registry that loads in production with `dev_key_in_use=False` (probe J5). |
| CI gates (NFR-SEC-02) | ci.yml runs lint, typecheck, schema drift, policy invariants, secret scan, bandit/pip-audit **advisory (`\|\| true`)**, production-refusal check, tests, determinism gate, evidence, SBOM [Verified: file read] | PARTIAL | SAST/SCA are non-blocking; no artefact/image signing; no DAST (O-23 open, needed by Gate B). SBOM present but unsigned (AEI #12). |
| Identity / maker-checker (FR-01) | Probe D: same person refused, same line refused, different line accepted, cooling period enforced, agent refused as maker/checker; Kill Switch two-person different-line; HALTED exit two-person different-line and never straight to autonomy; break tickets two-person [Verified] | MET (dev/sim) with a defect | IVA-05: `MakerChecker.reject()` has no human/maker guard — an AGENT actor rejected a pending limit change (probe B3). |
| Authentication at the edge (R-06) | BFF principals are client-asserted headers; a caller asserting `X-Actor-Role: sre_lead` activated the PLATFORM Kill Switch (HTTP 202) (probe B6); `dev_header_auth_allowed()` returns True when `RT_ENV` is unset [Verified: app.py lines 42-44; probe B6] | NOT MET (by design in sim) | Documented as R-06 "needed by Gate B, Open". The unset-env default is fail-open, unlike the registry loader which fails closed on unset env. IVA-06. |
| Tenancy (NFR-TEN-01) | Cross-tenant approver refused (probe D5); allowlist per tenant; STRATEGY/INSTRUMENT limits must be tenant-qualified (policy.py lines 136-139) [Verified] | PARTIAL | RT-03 tenant-escape red team open; single sim tenant only. |
| Session packets / independent reviews (AEI #13) | SESSIONS/C01..C12, P1..P6, REVIEW_C2, C3, C4, C5 exist; REVIEW_C4 probe table P1..P22 with remediation references [Verified: file reads] | MET (dev/sim) — self-generated | All four "independent" reviews were authored inside the same session as the build (commit trailer `Claude-Session: …session_0183BSTLaUBhYYhEhbSKc5Xb` on every commit). They are challenge packets, not 3rd-line evidence. |

## 3. Independent re-verification of the mandated claims

All probes are throwaway scripts under the session scratchpad (`probe_a_…` to `probe_j_…`), executed against HEAD with the Makefile PYTHONPATH and `RT_ENV=sim`. "Expected" means the behaviour the blueprint requires; a line marked DEFECT/OBSERVATION is a deviation I found. Counts are my own checks, not builder tests.

| # | Claim | What I did | Result | Verdict |
|---|---|---|---|---|
| (a) | Risk engine determinism | `decide()` 50× in-process; 3 child interpreters with PYTHONHASHSEED 0/12345/random and reversed `limits`, `positions`, `open_orders` lists; wall-clock sleep between calls | Byte-identical DecisionRecord and identical `engine_build_hash` in every case (24/24) | MET (dev/sim) |
| (a) | Fail closed on missing/stale inputs | None for each of intent/policy/account/market; account `as_of` 6 s old and 2 s in the future; market 30 s old; quality STALE; provenance news_adapter; future market_ts; limit removed; freshness table emptied; all limits emptied; NAV 0; a check raising ZeroDivisionError | HALTED RK-HALT-INPUT for all missing inputs, stale/future account snapshot and the engine exception; REJECTED RK-FRESH*/‑UNDEFINED/RK-NAV for the rest; nothing APPROVED | MET (dev/sim) — note stale *market* data is REJECTED while stale *account* data is HALTED, consistent with RISK_POLICY.md |
| (b) | AI/MCP identity cannot activate/deactivate the Kill Switch | Actors: kind AGENT role strategy_agent; AGENT spoofing sre_lead; AGENT with runtime_monitor role; SYSTEM with strategy_agent; HUMAN with strategy_agent role; SYSTEM with sre_lead (deactivate) | All refused with ControlDenied; five `killswitch.denied` audit rows; activation remained active | MET (dev/sim) at the service layer |
| (b) | …cannot change a limit | AGENT propose → refused; AGENT check → refused; AGENT spoofing a 1st-line role as checker → refused; MCP calls to `change_limit` etc. → TOOL_NOT_REGISTERED; policy version unchanged | MET, **except** `MakerChecker.reject()` accepted an AGENT actor (IVA-05) | PARTIAL |
| (b) | …cannot reach the execution gateway from the analytics plane | `gateway.submit` inside `enter(Plane.ANALYTICS)` and from an unattributed context | PlaneViolation both times, recorded on the guard (fail closed) | MET (dev/sim); but `cancel`/`cancel_all` carry no plane check (IVA-10), and a caller that itself enters CONTROL is accepted (IVA-11) |
| (b) | BFF path | Bearer-only POST /v1/killswitch → 401; asserted `strategy_agent` → 403; asserted `sre_lead` → **202 activated** | Header identity is client-trusted (IVA-06, R-06) | NOT MET outside sim by design |
| (c) | Kill Switch supersedes an approved intent | Supervised account; human approval recorded; switch engaged at each of the six levels; `pipeline.on_approval` | Blocked at all six levels with RK-HALT-KS on re-decision; 0 broker submissions; intent state HALTED. Autonomous intent under an active switch → HALTED | MET (dev/sim) on the pipeline path |
| (c) | …and a command already authorised before the switch | Genuine APPROVED decision; OrderCommand built exactly as the pipeline builds it; ACCOUNT switch engaged (account HALTED, lease released); a restarted executor re-acquires a lease and calls `gateway.submit` | **Order ACKNOWLEDGED by the broker while the switch is active** (`broker_submissions=1`, `ks_active=True`). The gateway consults neither Kill Switch flags nor account mode; lease preemption is transient | **NOT MET** on the target topology (IVA-01) |
| (d) | Maker-checker refuses same-person and same-line | Limits: same person, same line (2nd/2nd, twice), 1st/1st; Kill Switch deactivation; HALTED exit; break tickets | All refused; different person + different line accepted; cooling period enforced; re-check of an EFFECTIVE change refused | MET (dev/sim) |
| (e) | Audit tamper and truncation detection | Payload tamper; header tamper; tamper + self re-hash; middle deletion; tail truncation with/without anchor; tail replacement with/without anchor; JSONL rewrite by owner process; backdated append | All detected at the correct sequence **when the anchor is supplied**; truncation and tail replacement are undetectable without it; JSONL rewrite reloads as a valid prefix; backdating coerced to monotonic time | MET (dev/sim) with IVA-09 |
| (f) | Replayed signed MCP call refused | Same signature twice (runtime and HTTP); same nonce with other args / other tool; signature 61 s old / 61 s in the future; altered nonce; write-class replay; re-signed identical intent | All refused (IDENTITY / INTENT_SCHEMA), queue depth unchanged, denial audited with call hash. **After clearing the in-memory nonce set (restart/replica) the same signature is accepted again within 60 s** | MET (dev/sim) with IVA-08 |
| (g) | Registry verification in production fails | `--production` with labels production/paper/shadow/pilot; RT_ENV unset; dev key via env var; self-signed "approved" registry; tampered signed file | All refused as expected, except: dev key via env var on a production-tagged non-fixture registry **loads** (IVA-07); any key string self-signs an "approved" registry (symmetric HMAC, O-22) | MET (script) with IVA-07 |
| (h) | Risk-reducing exemption cannot increase exposure | At-cap long 1000: SELL 3000 (flip to −2000), SELL 1500 (flip to −500); at-cap short −1000: BUY 3000, BUY 1500, BUY 500, BUY_TO_COVER 500; correlated group; flag off | Flips are never marked risk-reducing; flips beyond cap REJECTED with the exposure codes; partial covers APPROVED via exemption; increasing a group member over cap REJECTED; with the flag off a genuine reduce on an over-cap book is REJECTED | MET (dev/sim) for the exemption itself |
| (h) | …order-splitting through resting orders | Flat book + resting BUY 1000 + new BUY 1000 (true projected 200 % of cap); long 1000 + resting SELL 1000 + new SELL 1500 (true projected −150 %) | **`max_position_per_instrument` PASSES both** — the check and its exemption use positions only and ignore open orders; `aggregate_open_notional` compares to `max_notional_per_order`, not the position cap (IVA-03) | NOT MET for max_position_per_instrument |

Builder-test coverage judgement (adversarial reading of test bodies): TC-RK-017 covers resting *buys* against gross exposure and a *shrinking* SELL, but not a flip nor resting orders against the position cap. TC-AP-004 covers the switch after approval only at ACCOUNT level via the pipeline; nothing exercises the gateway with an already-authorised command. TC-AI-009 covers replay in one process only. TC-AUD-005 covers truncation with the anchor and states the no-anchor limitation in its docstring. TC-ID-001 covers maker≠checker and different line but not `reject()`. The identity/"agent cannot" tests are all service-layer; none exercises the BFF with spoofed headers.

## 4. Findings

### Veto grounds under the Gate B minimum
None met at role level: every critical control in RACI.md/CODEOWNERS has a named 2nd-line role. The absence of appointed persons (H-01) and enabled branch protection (O-20) is recorded as a condition, not a veto. [Verified]

### Conditions (must be closed before Gate C is convened)
| ID | Finding | Evidence | Owner (RACI) |
|---|---|---|---|
| IVA-01 | Execution gateway does not consult Kill Switch flags or account HALTED mode; a pre-authorised OrderCommand reached the broker after ACCOUNT-level activation. On the target bus topology (ADR-005, outbox `replay()` exists in services/oms/oms/outbox.py line 29) this is a real window. Architecture must specify KS/halt verification inside the execution plane, not only upstream. | probe C3; gateway.py `submit()` lines 118-190 (no KS/mode check) | Backend Lead / Trading Domain Lead; 2nd line Chief Risk Agent |
| IVA-02 | OrderCommand carries no signature and the gateway does not verify that `decision_id` exists or is APPROVED; a forged command (`decision_id='dec_forged'`, `authorised_by='FORGED'`) was ACKNOWLEDGED when the caller claimed the CONTROL plane. THREAT_MODEL T-08 says "signed intents"; DF-05 lists only idempotency + fencing. | probe B4; libs/core/rtcore/schemas/order.py lines 33-55 | Integration Architect; 2nd line Security Architect |
| IVA-05 | `MakerChecker.reject()` lacks the human/maker guards present on `propose()`/`check()`: an agent can veto a limit *tightening*. Not exposed via BFF or MCP today. | probe B3; makerchecker.py lines 87-95 | Backend Lead; 2nd line Chief Risk Agent |
| IVA-06 | BFF header-asserted principals (R-06): spoofed `sre_lead` activated the PLATFORM switch; `dev_header_auth_allowed()` is fail-open when `RT_ENV` is unset. Replace with IdP/MFA sessions; make unset env refuse to start. | probe B6; app.py lines 42-44, 95-111 | Backend Lead, Security Architect |
| IVA-07 | Dev registry key not black-listed: `RT_MCP_REGISTRY_KEY=<dev key>` signs/loads a production-tagged non-fixture registry with `dev_key_in_use=False`. Symmetric HMAC means any key-string holder can self-sign an "approved" registry. Resolve with O-22 (asymmetric/KMS) and an explicit dev-key deny-list. | probe J5, G4; registry.py `signing_key()` lines 112-120 | MCP Security Agent, Security Architect |
| IVA-11 | Plane guard is per-process; the enforced boundary is the Kubernetes NetworkPolicy set, which has been verified statically only and never applied (H-05). | probe B4; scripts/check_network_policies.py | Cloud Architect |
| IVA-12 | Threat model rows T-04, T-05, T-10, T-11, T-13 cite test IDs (TC-SC-*, TC-SEC-001, TC-PERF-004, TC-TEN-003) that do not exist; those controls are documents only. | `grep -rl` → 0 files | Security Architect |
| IVA-13 | ADR-001..012 all "Proposed"; threat model, data flows and capacity model unapproved by their boards; O-04, O-05, O-13, O-17, O-20, O-22, O-23, R-06 are "needed by Gate B" and open in RAID_LOG. | file reads | Program Orchestrator |
| IVA-14 | CI SAST/SCA advisory only (`\|\| true`); SBOM unsigned; no image/artefact signing (O-23). | ci.yml | Cloud Architect |

### Observations (non-blocking)
- IVA-08 Nonce store for signed calls is in-memory, per process and unbounded; revocations persist to JSONL, nonces do not (probe F5). Becomes a condition at Gate C.
- IVA-09 Audit anchor (`seal()`) is produced by the same process; no out-of-band publication is implemented; the JSONL "WORM" backend is append-only by convention only (probe E5/E6/E8). Becomes a condition at Gate C.
- IVA-10 `ExecutionGateway.cancel`/`cancel_all` have no plane check (only `submit` does); mitigated by the fencing-token requirement.
- IVA-15 `ToolRuntime.restore_registry` requires "two distinct approver strings", not two human Actors from different lines (runtime.py lines 108-116).
- IVA-16 The "independent" review packets in docs/SESSIONS/REVIEW_* were produced in the same session as the build; treat them as 1st-line challenge, not 3rd-line assurance.
- IVA-17 Evidence provenance: EVIDENCE_REPORT.md/TC-*.md embed the parent commit; BROKER_CERTIFICATIONS/sim-broker.md carries a hard-coded timestamp (scripts/certify_broker.py line 18: `now = datetime(2026, 9, 7, 14, 0)`), not the run time.
- IVA-18 Working tree was modified by another process during validation (GATE_A §1); the head is moving while being validated.

## 5. Verdict

**ACCEPT WITH CONDITIONS** (dev/sim evidence only) — as a recommendation, and only if the Committee first closes Gate A: the entry criterion "Gate A passed" is not met, so by the kit's procedure this gate cannot be convened today and this verdict is informational until then.

Grounds: the architecture that the blueprint requires — three planes with a deny-by-default topology, deterministic engines with no I/O, executor lease with fencing, signed tool registry with the six-tool enum, hash-chained audit, maker-checker with cooling period — exists and behaves as specified in dev/sim under my own adversarial probes. The Gate B minimum veto ground is not met at role level. However, the exit evidence is unapproved (all ADRs Proposed, no board minutes), the capacity model has no numbers by design, five threat rows have no test, and I found two architectural gaps in the execution-plane trust model (IVA-01, IVA-02) plus an edge-authentication gap (IVA-06) and a signing-key gap (IVA-07) that must be designed out before any environment beyond sim.

Passing Gate B authorises only the development and simulation environments; it enables no market, strategy or autonomy [Source: 12]. This is a recommendation; the human approver of record is pending.

## 6. Sign-off

| Role | Name | Line | Signature | Date |
|---|---|---|---|---|
| Author | Independent Validation Agent (AI) | 3rd | — (AI output; never self-certifies) | 2026-09-07 |
| Reviewer | pending | | | |
| Approver | pending (Committee chair) | | | |
