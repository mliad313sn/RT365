# SECURITY_PLAN

| Owner (author of record) | Reviewer (different line) | Recommending body | Decider | First gate | Status |
|---|---|---|---|---|---|
| Security Architect | MCP Security Agent (2nd line) — **SIGN WITH FINDINGS**, packet `docs/SESSIONS/REVIEW_2026-09-08_security_plan_mcp.md` (SP-C9) [Source: docs/SESSIONS/REVIEW_2026-09-08_security_plan_mcp.md:5] | Security & Privacy Board (recommends; COUNCIL_2026-09-08_gate_B_spb_docs §2.5) | Product Owner (human, D-039; AI delegate under D-040) | B | v1.2 — the reviewer's five corrections and the header correction applied; **not accepted**; decision pending (D-067) |

Version note [Committee: COUNCIL_2026-09-08_gate_B_spb_docs §2.5; D-061 (2)]: v1.0 was **not accepted** — it presented targets as implementation, named verifiers that have not run, and linked no evidence. v1.1 applies SP-C1..SP-C9: every control is split into what blueprint 06 asks for, what exists in dev/sim with its test ids and paths, the gap with its RAID id, the verifier that will eventually prove it, the evidence that exists today, and the gate at which the control is first required. No status of "accepted" is recorded here: D-061 states that no document status changes to accepted until its new version *and* its reviewer signature exist, and the author of this document is not its approver. Nothing in this plan authorises any environment beyond dev/sim [Source: 12]. Profit is an objective, never a promise [Source: 00].

**What changed in v1.2, and why** [Committee: D-067 — "each author issues the small corrective version their reviewer named"; `docs/DECISION_LOG.md:75`]. The MCP Security Agent signed v1.1 **with findings** and named five corrections of fact, each demonstrated in the packet, plus a header that cited a packet filename which does not exist. All six are applied here, each re-verified against the tree at this commit rather than taken from the packet: **MSA-F-1** the environment-ladder row claimed that anything unlabelled fails closed, and the registry loader skips the environment-tag check when `RT_ENV` is unset — still true at `mcp/servers/mcp_servers/registry.py:303`; **MSA-F-2** two rows called the MCP handler deadline "pre-emptive", and it releases the caller without stopping the handler — still true at `mcp/servers/mcp_servers/runtime.py:254-256`; **MSA-F-3** `secrets_mount: none` was offered as a delivered control against a network test, and it is a declaration checked for self-consistency; **MSA-F-4** two rows named the trust-set RAID items but not O-141, the one that says the file does not exist — `mcp/policies/trust/` is still absent from the tree; **MSA-F-5** the registry row promised "a drilled revocation path" without recording that no drill has ever been run [Source: mcp/policies/README.md:13]; **MSA-F-9** the header cited `REVIEW_<date>_security_plan_mcp_security_agent.md`, and the packet is `docs/SESSIONS/REVIEW_2026-09-08_security_plan_mcp.md`. One further correction of fact was made in the same two rows, because leaving it would have restated something the tree now contradicts: v1.1 said the `EgressPolicy` was loaded but not on the tool call path, which was true when the reviewer wrote it and is not true now (D-068) — both rows are corrected with the code and the tests. The reviewer's advisory findings (MSA-F-6, MSA-F-7, MSA-F-8, MSA-F-10..F-12) are **not** actioned by this version and remain owed [Open: REVIEW_2026-09-08_security_plan_mcp §4]. This version records no approval, no risk acceptance and no gate pass.

**Reading rules.**
- **"Delivered in dev/sim" means a test exists and passes at this commit.** A test id that appears without a `[Open]` tag exists in `docs/TEST_CASES/` and in the test tree; a test id tagged `[Open]` **does not exist** and is not evidence.
- **"Target verifier" is who will prove the control when the control and the verifier both exist.** No pen-test, red-team engagement, chaos run or DR drill has taken place [Open: H-10, H-19]; naming a verifier is not evidence that it ran.
- No threshold, regulatory status, vendor capability, provider term or price is asserted anywhere in this plan; every such point is `[Open]` with the source that would settle it.

## Controls [Source: 06]
| Control | Target [Source: 06] | Delivered in dev/sim (tests, paths) | Gap [Open: id] | Target verifier | Current evidence | First gate |
|---|---|---|---|---|---|---|
| Zero trust, mTLS, workload identity | Mesh with mTLS on the Control and Execution planes, workload identity per service, no trust from network position (ADR-006) | In-process `PlaneGuard` refusing cross-plane calls with S1 `plane.deny`; Kubernetes NetworkPolicy manifests with an effective-union checker (`scripts/check_network_policies.py`, `make policy-check`) (TC-NET-001..004) | No cluster, no mesh, no mTLS, no workload identity [Open: H-05]; NetworkPolicy allow-union semantics verified by reading only [Open: O-42]; cluster conformance [Open: IVA-11]; **`secrets_mount: none` is not a delivered control and TC-NET is not its evidence [Committee: REVIEW_2026-09-08_security_plan_mcp MSA-F-3]** — it is one line of `mcp/policies/runtime.yaml` compared with the literal string `none` by `scripts/verify_tool_registry.py:80` and `apps/cli/rt365_cli/main.py:119`, a declaration checked for self-consistency; **no runtime enforces it**, `docs/TEST_CASES/TC-NET.md` carries no secrets case, no Kubernetes manifest sets it, and in dev/sim the MCP runtime shares a process with the BFF on a writable filesystem [Open: H-05] | External pen-test [Open: H-10]; Cloud Architect on the cluster | `docs/TEST_CASES/TC-NET.md`; `check_network_policies.py` output in CI; THREAT_MODEL T-20, T-21 | B (in-process), C (cluster) |
| MFA/passkeys, PIM, separation of duties | IdP-issued sessions with MFA/passkeys, privileged-access management windows, maker-checker and different-line rules bound to authenticated subjects | Maker-checker and two-person primitives (TC-ID-004, TC-AP-005); role and permission matrix for 17 roles with BFF 403s (TC-ID-006); non-human roles refused on the human path and the BFF refusing to start outside `RT_ENV=sim` (TC-E2E-AUTH, TC-ID-005) | Identity is client-asserted headers; `mfa_enrolled` and `privileged_until` are hard-coded; one human holding two header identities satisfies the two-person rule [Open: R-06, R-30]; no IdP, no MFA, no passkeys, no real PIM window | External red team [Open: H-10]; IVA on the segregation record | `docs/TEST_CASES/TC-ID.md`, `TC-AP.md`; THREAT_MODEL T-06, T-07, T-14, T-15 | B (primitives), C (IdP) |
| Signed artefacts, SBOM, SAST/DAST/SCA, secret scanning | CI gates on SAST, SCA, DAST and secrets; every deployable artefact carries a signed SBOM; an unsigned deploy is refused | Gating secret scan (`scripts/secret_scan.py`, `make secret-scan`); gating SAST (`bandit`, HIGH severity at MEDIUM+ confidence) and gating SCA (`pip-audit` against `requirements.lock.txt`) in `make security-scan` (D-054); CycloneDX SBOM generated from the declared closure (`scripts/generate_sbom.py`) | SBOM is **unsigned**; no cosign signature, no SLSA provenance, no installer signature verification [Open: D-054 → O-83]; no `--require-hashes` install; GitHub actions not SHA-pinned; no DAST; no image is built or signed [Open: O-84]; **nothing in the platform refuses an unsigned deploy today**; TC-SC-001..003 do not exist [Open: O-83] | QA Lead and Cloud Architect on the CI gates; external pen-test on the installer path [Open: H-10] | `.github/workflows/ci.yml` (`security-scan`, `secret-scan` steps); `security/sbom/sbom.cdx.json`; RAID O-23 note; THREAT_MODEL T-04, T-10 | B (scanners), C (signing) |
| Encryption and tokenisation | KMS-managed keys, encryption in transit and at rest, tokenised identifiers in analytics | Masked account state to AI tools, no identifiers in an AI context (TC-AI-001, TC-AI-012); account/customer id, IBAN and IPv4 redaction at log emission (TC-OB-002); no secret literal in the tree (secret scan) | No KMS or HSM and no key custody [Open: H-20, O-53]; **no encryption at rest** — the control store, the audit store and the JSONL journals are plaintext files on the host (B10); transport encryption is a deployment property that does not exist [Open: H-05]; no tokenisation service; structured field-level redaction missing [Open: R-35] | External pen-test [Open: H-10]; Cloud Architect (KMS); Privacy Lead (tokenisation) | `docs/TEST_CASES/TC-AI.md`, `TC-OB.md`; THREAT_MODEL T-05, T-24 | C |
| Signing keys: custody, ceremony, rotation, one key per purpose | One non-exportable KMS/HSM key pair per purpose (tool registry, order-command authorisation, risk policy); verifiers hold public material only; three-person ceremony recorded; overlap-then-retire rotation (D-053, ADR-019) | `rtcore.trust` public `TrustSet` (key_id, valid_from, valid_until, revoked, ceremony reference); `rtcore.signing` banned from `mcp_servers` (TC-AI-005); algorithm allowlist before any cryptographic operation; purpose and key_id bound into the signed bytes; verify-only handles (`PublicKeyCommandVerifier`, registry `TrustSet`); `retire()` absolute; validity checked at `authorised_at` (TC-SIG-001..004); `security/signing/README.md`, `trust_set.schema.json`, `ceremonies/TEMPLATE.md` | **No ceremony has run and no production key exists** [Open: H-20]; the committed registry is still dev-HMAC signed under `dev-key-v0`, refused outside dev/sim [Open: O-127]; key custody at start-up [Open: O-53]; **the trust-set file `mcp/policies/trust/registry_keys.json` does not exist in the tree at all — not merely undistributed [Open: O-141; Committee: REVIEW_2026-09-08_security_plan_mcp MSA-F-4]**, and an absent anchor means the asymmetric path refuses everything with `TRUST-ANCHOR-ABSENT` [Source: libs/core/rtcore/trust.py:190], which is fail closed and correct and is the reason **nothing asymmetric can be exercised end to end today**; trust-set distribution through runbook, CODEOWNERS and installer [Open: O-126]; the pure-Python Ed25519 verifier is unmeasured against the latency budget [Open: O-125]; the rotation interval and overlap in D-053 are proposals, not decided values; ECDSA P-256 not implemented [Open: Q-22-1] | MCP Security Agent (2nd line); the Board witnesses the ceremony record; IVA | `docs/TEST_CASES/TC-SIG.md`; `security/signing/`; ADR-019; THREAT_MODEL T-69..T-73 | B (code path), C (ceremony) |
| Tenant isolation | Partitioned streams, storage and caches per tenant; tenant as an authenticated claim | Server-side tenant binding with the forged `X-Actor-Tenant` header refused (403) and audited; per-tenant allowlist files with a name/content cross-check; tenant predicate on every read; 404 for foreign objects; persisted tenant-wide revocation with no collateral auto-action; tenant-qualified limits (TC-TEN-001..004, TC-RK-018) | No partitioned streams or storage — one control store and one audit store per platform [Open: O-115, O-136]; tenant is not yet an authenticated claim [Open: R-06]; the cross-tenant Kill Switch blast radius is an accepted risk pending Trading Risk Committee confirmation [Open: R-57, O-132]; RT-03 has been run only as TC-TEN in dev/sim, never against a deployment [Open: H-10] | External red team (RT-03) [Open: H-10]; Backend Lead and Chief Risk Agent on R-57 | `docs/TEST_CASES/TC-TEN.md`; `docs/RED_TEAM_PLAN.md` RT-03; THREAT_MODEL T-17, T-64..T-68 | B (application level), C (storage partitioning) |
| Platform-operator and platform-auditor principals | Tenant-less, time-bound IdP claims on a separate route family; PIM window; two-person deactivation of platform-wide levels; every unfiltered auditor read itself audited and pseudonymised by default (D-059, D-061 (7)) | The tenant BFF refuses platform levels for every tenant-bound principal (403, fail closed, TC-TEN-003); no unfiltered read path exists; platform-wide rows carry tenant `-` | Both designs are owed [Open: O-120, O-121]; there is **no human platform path at all today** (R-56); the residency question for a cross-tenant read is unanswered and no jurisdiction's law is asserted [Open: O-10]; no header- or CLI-based platform identity may be added as a stop-gap (D-061 (7)) | Privacy Lead (envelope); Security Architect; IVA as the consumer of the auditor view | `docs/TEST_CASES/TC-TEN.md`; THREAT_MODEL T-82, T-83; BUILD_E01 C-2/C-7 | C |
| Secure SDLC | DoR/DoD security items, protected paths under 2nd-line approval, tests-first control quartets, an RTM row per story | DoD checklist in every PR; `.github/CODEOWNERS` protected paths; control quartets for control-bearing code; `make all` run in CI on every push; ADR, RAID, decision and RTM ledgers maintained | CODEOWNERS team handles are placeholders that map to no reviewer [Open: O-20]; branch protection is not enabled [Open: H-01]; no CI path-ownership or authorship check exists [Open: O-85, O-131]; the evidence report of record was generated against a moving tree [Open: O-65] | IVA; Program Orchestrator | `.github/CODEOWNERS`; `.github/workflows/ci.yml`; `docs/TEST_CASES/EVIDENCE_REPORT.md` (with the O-65 caveat) | B (practice), C (enforcement) |
| Fail-closed distribution (installer, wheel, one-file executable) | A distributed build refuses to run unlabelled, refuses a tampered or incomplete bundle, and verifies a signature before use | `rtcore.resources` marker check per candidate root; explicit `RT365_HOME` never falls back; environment label required and only sim servable; fixture registry refused outside dev/sim; SHA-256 per artefact; bundled tzdata (TC-PKG-001..005); release workflow smoke test | Cosign signing, SLSA provenance and installer signature verification [Open: D-054 → O-83]; OS-native code signing (Authenticode, notarisation) [Open: H-30]. The v1.0 tag "signing [Open: O-23]" is superseded (SP-C7) | TC-PKG quartet and the release workflow; SRE Lead; external pen-test on the installer [Open: H-10] | `docs/TEST_CASES/TC-PKG.md`; release workflow run recorded in AUDIT_EVIDENCE_INDEX; THREAT_MODEL T-49, T-50 | B (fail-closed), C (signature) |
| Agent segregation (three lines for delivery agents) | Roster-generated agents with segregation machine-checked, so no agent authors what it reviews or approves | Generated roster (67 agents, `make agents-check`); PreToolUse hook on Edit/Write/MultiEdit/NotebookEdit using the harness-supplied `agent_type` (PROBE_O58); TC-AGT-001..004 prove **roster consistency and the Edit/Write path only** | **Bash writes are uninspected (R-46) and are the default path under harness auto mode (R-54); the guard modules and the protected service paths are inside every builder's roster scope, so the guard is editable by its own subjects [Open: O-131 roster narrowing]**; CODEOWNERS handles are placeholders [Open: O-20]; no branch protection [Open: H-01]; no CI authorship check [Open: O-85]. **The control of record is CODEOWNERS plus branch protection plus the CI path-ownership check, none of which is complete; TC-AGT may never be cited as proof that segregation is enforced** | IVA authorship review; the CI path-ownership check when it exists (O-131) | `docs/TEST_CASES/TC-AGT.md`; `docs/SESSIONS/PROBE_O58_2026-09-08.md`; COUNCIL_2026-09-08_gate_B_spb_docs §3.7; THREAT_MODEL T-48 | B (guard rail), C (control of record) |
| MCP registry and egress | A signed tool registry, forbidden capabilities structurally impossible, per-plane and per-tenant egress allowlists, a drilled revocation path | Envelope-signed registry with environment tag, fixture flag and dev-key black-list outside dev/sim; `verify_tool_registry.py --production` refusal asserted in CI; asymmetric verification path against a public trust set (TC-AI-005/010/011, TC-SIG-001..004); stdio transport identity bound by the host process with client `_meta` ignored (TC-AI-012..015); per-tenant allowlists (TC-TEN-001..004); network-policy invariant checker (TC-NET-001..004); emergency revocation and quota denial paths (TC-AI-003, TC-TEN-004); the egress allowlist bound per tool call as an `EgressGuard`, with deny-all when no policy is loaded and outside a call, and no network module anywhere in the handlers' first-party import closure (TC-AI-024..027) | The committed registry is still dev-signed [Open: O-127]; **the trust set the asymmetric path verifies against does not exist in the tree [Open: O-141]**; **no revocation drill has ever been run — the TC-AI recovery cases are tests, not a drill [Open: H-19; Source: mcp/policies/README.md:13]**; cluster conformance of the NetworkPolicies [Open: O-42, IVA-11]; per-tenant allowlists are not covered by the registry signature [Open: O-81, O-123]. **Corrected in v1.2:** v1.1 said the in-process `EgressPolicy` was loaded but not on the tool call path; it now is [Source: mcp/servers/mcp_servers/runtime.py:250; apps/web/web_bff/platform.py:1132] and the honest control is the absence of the capability rather than a filter on traffic — the guard authorises a destination, it never carries the connection, so it evidences nothing about a deployed network [Open: H-05] | MCP Security Agent (2nd line); external pen-test [Open: H-10] | `verify_tool_registry.py` output quoted in COUNCIL_2026-09-08_gate_B_spb_docs §0.4; `docs/TEST_CASES/TC-AI.md`, `TC-NET.md`, `TC-SIG.md`; THREAT_MODEL T-29, T-47 | B |
| Model-provider boundary | One Analytics-plane gateway holding the only provider credential; processing region pinned to the tenant `residency_region`; masking and provenance labels at the boundary; egress allowlist in code and in NetworkPolicy; fail closed to Observe on outage (D-052) | **Nothing.** No gateway service, no provider, no credential, no terms | The whole control [Open: O-79]; no provider is named, no terms are signed, no provider capability, retention policy, region or price is asserted [Open: H-06, Q-05-1..14]; TC-AI-016..019 do not exist [Open: O-79]; the Model Risk Committee has not been heard [Open: O-101] | Security Architect and MCP Security Agent on the design; external pen-test before any live provider [Open: H-10]; Board review of signed terms before wiring (D-052) | ADR-018 is *not* the model-gateway ADR; the decision text is D-052; THREAT_MODEL T-59 | B (design), D (provider live) |
| WAF/DDoS and egress control | Edge protection plus per-plane egress allowlists that deny by default | Per-plane NetworkPolicy manifests with the effective-union checker refusing 0.0.0.0/0 and refusing an analytics→execution route (TC-NET-001..004); MCP deny bucket and per-identity quota (TC-AI-003) | A per-handler deadline exists in code but has no test [Open: R-20], and it is **not pre-emptive** [Committee: REVIEW_2026-09-08_security_plan_mcp MSA-F-2]: it is a caller-side deadline that releases the caller and discards the result; the handler thread is not interrupted and continues to hold one of four pool workers [Source: mcp/servers/mcp_servers/runtime.py:254-256, 90, 107], so four non-returning handlers exhaust the pool and every later tool call waits out its own deadline before being denied [Open: R-20]; no edge and no WAF exist [Open: H-05]; **corrected in v1.2** — the in-process egress allowlist is now on the tool call path as an `EgressGuard` bound per call (TC-AI-024..027) [Source: mcp/servers/mcp_servers/runtime.py:250], which is a control on a *future* handler's acquisition of a client and not a filter on traffic; the enforced network half remains manifests for a cluster that does not exist [Open: H-05, REVIEW_C3 F-20]; per-adapter broker CIDRs are placeholder values; no DNS egress monitoring [Open: R-32]; TC-PERF-004 and TC-NET-006 do not exist [Open: R-35, R-32] | Performance & Chaos Lead; Cloud Architect; external pen-test [Open: H-10] | `docs/TEST_CASES/TC-NET.md`; THREAT_MODEL T-11, T-20, T-21 | B (policy files), C (edge) |
| Rate limiting per principal | A per-principal request and intent rate limit, with pre-validation floods never amplified into audit events | MCP path only: deny bucket plus per-identity quota (TC-AI-003) | No rate limit on the human/BFF path; a flood of malformed intents still writes audit events [Open: R-35]; TC-PERF-004 does not exist | SRE Lead; Performance & Chaos Lead | `docs/TEST_CASES/TC-AI.md`; THREAT_MODEL T-25, T-11 | C |
| Replay defence (nonces, one-shot grants, durable journals) | Every signed artefact is single-use and survives a restart: nonces, one-shot authorisation grants, revocation records | Nonce plus issued_at in the tool-call signature with a per-token nonce cache and a persisted nonce journal; the queue refuses a replayed `intent_id`; one-shot command MAC with a bounded authorisation age, one order per decision and a consumed-grant table that survives restart (TC-AI-009, TC-AI-011, TC-AI-008, TC-RK-015, TC-EX-010, TC-DUR-001, TC-EX-011) | The revocation and nonce journals are plain append-only JSONL with **no digest and no chain**, so a deleted line silently un-revokes or re-enables a nonce [Open: O-55, O-118, O-128]; a restore that pairs a control store with the wrong journals is a replay [Open: O-118, H-19] | MCP Security Agent (revocation semantics); external red team [Open: H-10] | `docs/TEST_CASES/TC-AI.md`, `TC-EX.md`, `TC-DUR.md`; THREAT_MODEL T-31, T-43, T-45, T-80, T-81 | B (in dev/sim), C (journals on the seam) |
| Kill Switch listener isolation | The Kill Switch path is reachable when any other handler is hung or saturated | A handler deadline with `mcp.handler_timeout` and the BFF calling the MCP runtime off the event loop exist **in code**; TC-AI-010 covers handler errors only | **No test covers the handler timeout** [Open: R-20]; **the deadline is not pre-emptive [Committee: REVIEW_2026-09-08_security_plan_mcp MSA-F-2]** — `future.result(timeout=...)` releases the caller and `future.cancel()` cannot stop a task that has already started, so the handler runs to completion holding one of four pool workers [Source: mcp/servers/mcp_servers/runtime.py:254-256, 90, 107]; that makes the deadline a correction of the caller's behaviour, not of the handler's, and pool exhaustion is the residual [Open: R-20]; the Kill Switch listener is not isolated from the MCP handlers [Open: R-20 residual]; TC-SEC-004 (id allocated in THREAT_MODEL v1.1) does not exist | Performance & Chaos Lead; SRE Lead | `docs/TEST_CASES/TC-AI.md`; THREAT_MODEL T-35 | C |
| Immutable logs (durable, witnessed audit) | WORM audit with a hash chain, restricted delete (none), and an anchor a compromised platform cannot rewrite | Audit events are durable rows on the `rtcore.store` seam in their own store file; per-row digests and a chained journal; the audit hash chain re-verified at open and refused with `AuditIntegrityError`; **no update and no delete API at any layer**; monotonic timestamps; sealed heads published to a directory the audit process does not own, as anchor records forming their own chain; truncation, stale backup, head mismatch, missing or broken witness fail closed with catalogued S1 alerts and exactly one incident row (TC-AUD-001..009) | No WORM medium; in dev/sim the "different principal" is a directory on the same host — the deployment identity with its own write credential is owed [Open: O-54]; anchor cadence and staleness ceiling are placeholders, not decided values [Open: O-133]; witness-recovery runbook [Open: O-134]; retention, legal hold and erasure against an append-only chain are unanswered [Open: O-135]; the platform-wide Kill Switch on a chain failure needs deliberate Trading Risk Committee confirmation [Open: O-137] | IVA; SRE drill [Open: H-19]; external pen-test [Open: H-10] | `docs/TEST_CASES/TC-AUD.md`; ADR-020; `docs/ALERT_CATALOG.md` `audit.anchor_missing`; THREAT_MODEL T-12, T-16, T-74..T-79 | B (dev/sim), C (deployment witness) |
| Durable control-state integrity | Control state that a host-level actor cannot rewrite undetected, on whatever store technology is deployed | `rtcore.store` seam with per-row SHA-256 digests checked on every read, a hash-chained journal replayed against the state at open, fail-closed `StoreIntegrityError` / `StoreError`, and S1 `execution.store_unavailable` so nothing reaches a broker on a store failure (TC-DUR-001..004, TC-EX-011, TC-KS-010) | Digests are **unkeyed**: a consistent rewrite of rows plus journal is undetectable by construction, and TC-DUR-003 does not attempt it [Open: O-110, RT-05]. The chosen remedy (D-061 (5), O-128) is to anchor the journal head into the audit chain whose head is anchored off-box by a different principal; the audit half is delivered (ADR-020) and **the journal-head anchoring is not implemented**; OS-level file hygiene is mandatory and non-evidential [Open: O-128]; journal compaction [Open: O-111]; multi-process, crash and power-loss behaviour unverified [Open: O-117]; the design must be re-validated on the Postgres adapter [Open: R-05] | IVA; external red team RT-05 [Open: H-10]; Performance & Chaos Lead (O-117) | `docs/TEST_CASES/TC-DUR.md`; ADR-018; THREAT_MODEL T-60..T-63, T-85 | B (dev/sim), C (anchored) |
| Backup and restore of control state; incident response | A restore never replays: the control tables, the revocation and nonce journals and the audit trail are restored as declared units and verified before the platform serves | DR_PLAN and INCIDENT_RESPONSE documents; the audit store is a separate file from the control store by design (ADR-020); with `store_dir` the MCP journals default to the control store's directory so one directory is one restore unit (dev/sim only, D-058 e) | **No drill has ever run** [Open: H-19]; the restore units are not written down in DR_PLAN and `verify()` against the last anchor does not gate serving [Open: O-118, O-128]; two store files now need different owners and schedules from shadow [Open: O-136]; witness recovery is not in the runbook [Open: O-134] | SRE drill with named operators [Open: H-19]; IVA | ADR-020; THREAT_MODEL T-81, T-86; `docs/DR_PLAN.md`, `docs/INCIDENT_RESPONSE.md` (documents, not drills) | C |
| Data handling: retention, legal hold, subject rights, privacy-safe telemetry | Purpose limitation, minimisation, residency and transfer mapping, retention schedules, deletion workflows, subject rights, DPIA where required, privacy-safe telemetry (PRIVACY_IMPACT) | Enclosing-scope legal-hold match with deletion suppressed when no schedule exists and human-only hold release (TC-CP-006); account/customer id, IBAN and IPv4 redaction at log emission (TC-OB-002); AI contexts receive masked account state only (TC-AI-004) | Structured field-level redaction of payloads and free-text reasons [Open: R-35]; DPIA per launch jurisdiction [Open: O-10]; erasure against an append-only audit chain has no answer (payload minimisation or crypto-shredding, neither exists) [Open: O-135]; residency of a platform-auditor cross-tenant read [Open: O-121, O-10]; **no legal basis, retention period or regulatory obligation is asserted anywhere** [Open: O-10] | Privacy Lead; Legal Agent; DPIA reviewer [Open: O-10] | `docs/PRIVACY_IMPACT.md`; `docs/TEST_CASES/TC-CP.md`, `TC-OB.md`; THREAT_MODEL T-23, T-24, T-83 | B (cross-reference), D (DPIA) |
| Scanner exceptions and risk acceptance | One exceptions file with an owner, a justification, a Product Owner decision reference and a bounded expiry; expired or unreferenced entries fail CI; inline suppressions forbidden (D-054) | `security/scan_exceptions.yaml` and `scripts/check_scan_exceptions.py` gating in `make security-scan`; currently 0 entries, none expired | The 90-day ceiling is the D-054 decision value and no exception has ever been exercised, so the negative and abuse cases are untested [Open: O-83, TC-SC-003]; no process exists for who may approve an exception other than the Product Owner decision reference the file requires | QA Lead; IVA on the decision references | `check_scan_exceptions.py` output quoted in COUNCIL_2026-09-08_gate_B_spb_docs §0.4; `security/scan_exceptions.yaml` | B |
| Environment ladder enforcement | An artefact or a process can only run in the environment it was authorised for; anything unlabelled or mislabelled fails closed | `RT_ENV == "sim"` exact match (no prefix or fallback); the BFF refuses to start outside sim; the CLI refuses an unlabelled environment; the fixture registry and the dev key are refused outside dev/sim from any source, including an environment variable; `verify_tool_registry.py --production` fails by design and CI asserts it (TC-ID-005, TC-E2E-AUTH, TC-AI-011, TC-PKG-002) | **The target property is not true of the registry loader in the unlabelled case [Committee: REVIEW_2026-09-08_security_plan_mcp MSA-F-1, probe P-1 §2.3].** `load_registry` binds `environment_tag` to `RT_ENV` **only when `RT_ENV` is set** — `if env is not None and tag != env ...` [Source: mcp/servers/mcp_servers/registry.py:303] — so with no environment label the tag check is skipped and an Ed25519-signed non-fixture registry of any tag, including `production`, passes it. It is unreachable today only because no Ed25519 registry and no trust set exist [Open: O-127, O-141], and it becomes live the moment either does; the HMAC and fixture paths do fail closed when unlabelled, because both treat `None` as not-a-dev-environment [Source: mcp/servers/mcp_servers/registry.py:156-161, 305]. A code change and a negative test are owed before O-127 closes [Open: MSA-F-1 — test owed, quartet drafted in REVIEW_2026-09-08_security_plan_mcp §6.1]; `build_sim_platform(second_tenant=True)` is not proven to be refused outside dev/sim [Open: O-123]; the ladder beyond sim has no environment to enforce against [Open: H-05] | MCP Security Agent; IVA at each gate | `docs/TEST_CASES/TC-ID.md`, `TC-PKG.md`, `TC-AI.md`; CI "registry must NOT be deployable to production" step; THREAT_MODEL T-42, T-50 | B |
| Red teaming and independent pen-test | An external red team and an independent penetration test before Gate D, scoped by someone other than the person who remediates | RED_TEAM_PLAN v1.0; RT-03 (tenant escape) executed in dev/sim as the TC-TEN quartet | **No external engagement is contracted and none has run** [Open: H-10]; RT-05 (consistent store and journal rewrite) is the case that would test T-61 and does not exist; the Security Architect must not scope and sign off the same test (this document's own author constraint) | External supplier; Red-Team & Pen-Test Lead scopes; Security & Privacy Board reviews the report; the Product Owner accepts the residual risk (D-039) | `docs/RED_TEAM_PLAN.md`; `docs/TEST_CASES/TC-TEN.md`; `docs/MISSING_ACTIONS.md` H-10 | D (pen-test), F (re-test) |

## What this plan does not claim [Committee]
1. **No control is claimed as verified by a verifier that has not run.** Every "Target verifier" cell names a future act; the "Current evidence" cell names what exists today.
2. **Twelve of the twenty-three rows carry a gap that no test covers.** That is the honest state of a dev/sim build; it does not block Gate B, which authorises dev/sim only [Source: 12]; it does mean no gate report may state that the control set is verified.
3. **No number in this plan is a decided threshold.** Rotation intervals, exception expiry, anchor cadence, lock timeouts and TTLs are decision values or placeholders carried from D-053, D-054 and ADR-020 with their RAID ids.
4. **No vendor, provider, jurisdiction or broker capability is asserted.** Where blueprint 06 names a control that depends on one (KMS, WAF, WORM storage, model provider, external pen-test supplier), the row says the dependency is `[Open]` with the human act that would settle it.
5. **The author of this document does not approve it.** Reviewer: MCP Security Agent (2nd line, SP-C9). Recommender: Security & Privacy Board. Decider: the human Product Owner (D-039), or the AI delegate under D-040 with the human's confirmation recorded. The Security Architect may not accept the residual risk of the controls it owns, and may not sign off a penetration test it scoped.

## Provenance
[Source: 06] for every "Target" cell. [Committee] for the row set and the conditions: `docs/SESSIONS/COUNCIL_2026-09-08_gate_B_spb_docs.md` §2 (SP-C1..SP-C9) and `COUNCIL_2026-09-08_gate_B_security_privacy.md` §1–§3 (D-052, D-053, D-054). Decisions cited: D-052, D-053, D-054, D-058, D-059, D-060, D-061, D-062. The "Model-provider boundary" and "Signing keys" rows were drafted by the Security Architect from the decision texts after the Board chair recused from wording rows that originated in the chair's own packet (SP-C8); the Independent Validation Agent is asked to review those two rows alongside the MCP Security Agent. Every "Delivered" cell was checked against the test tree and `docs/TEST_CASES/` at this commit; anything not found there is written as a gap, not as a delivery.
