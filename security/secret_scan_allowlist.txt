# Paths or literal strings the secret scanner ignores (fixtures that are documented as non-secrets).
mcp/servers/mcp_servers/registry.py:dev-only-registry-key-replace-before-gate-B
infra/docker-compose.yml:POSTGRES_PASSWORD
